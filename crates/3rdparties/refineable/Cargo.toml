[package]
name = "gpui-toolkit-refineable"
version = "0.1.0"
edition = "2024"
description = "A macro for creating 'refinement' types that can be used to partially initialize or mutate a complex struct"
license = "Apache-2.0"

[lib]
name = "refineable"
path = "src/refineable.rs"
doctest = false

[dependencies]
derive_refineable = { workspace = true }
