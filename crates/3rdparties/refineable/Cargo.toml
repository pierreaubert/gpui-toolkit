[package]
name = "refineable"
version = "0.1.0"
edition = "2024"
description = "A macro for creating 'refinement' types that can be used to partially initialize or mutate a complex struct"
license = "Apache-2.0"
publish = false

[lib]
path = "src/refineable.rs"
doctest = false

[dependencies]
derive_refineable = { version = "0.1.0", git = "https://github.com/zed-industries/zed.git", tag = "v1.9.0" }
