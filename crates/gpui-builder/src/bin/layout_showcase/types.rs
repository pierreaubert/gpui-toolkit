// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use gpui_builder::Axis;
use gpui_builder::SolvedNodeRef;
use gpui_builder::SolvedTree;

#[derive(Clone, Copy)]
pub(super) enum DragTarget {
    Sidebar,
    Inspector,
}

#[derive(Debug)]
pub(super) struct VisualTreeRow {
    pub(super) id: String,
    pub(super) depth: usize,
    pub(super) width: f32,
    pub(super) height: f32,
    pub(super) visible: bool,
    pub(super) resolved_axis: Option<Axis>,
    pub(super) active_tier: Option<String>,
}

pub(super) fn collect_visual_tree_rows(tree: &SolvedTree<'_>, rows: &mut Vec<VisualTreeRow>) {
    collect_visual_tree_rows_recursive(tree.root(), 0, rows);
}

fn collect_visual_tree_rows_recursive(
    node: SolvedNodeRef<'_, '_>,
    depth: usize,
    rows: &mut Vec<VisualTreeRow>,
) {
    rows.push(VisualTreeRow {
        id: node.id().to_string(),
        depth,
        width: node.width(),
        height: node.height(),
        visible: node.visible(),
        resolved_axis: node.resolved_axis(),
        active_tier: node.active_tier().map(str::to_string),
    });

    for child in node.children() {
        collect_visual_tree_rows_recursive(child, depth + 1, rows);
    }
}
