[build]
target = "index.html"
# The "Scatter (vello)" showcase section is cfg'd on the vello feature.
features = ["vello"]

[serve]
addresses = ["127.0.0.1"]
port = 8082
open = true
headers = { "Cross-Origin-Embedder-Policy" = "require-corp", "Cross-Origin-Opener-Policy" = "same-origin" }
