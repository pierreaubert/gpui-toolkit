# Unreleased

## Added

- Re-exported the executable release-gate API (`execute_packaging_entry`,
  `parse_executable_gate_command`) from the aggregate facade.

## Refactored

- Reordered the release-gate re-exports; no behavior change.
