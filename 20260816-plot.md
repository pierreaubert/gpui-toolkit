# MeshPlot implementation task list

## Post-draft evidence update

- Added `scripts/mesh_plot_visual_compare.py`, a dependency-free cross-adapter
  PNG comparator with exact or explicit antialiasing tolerances, canonical case
  mapping for WGPU/component-lab manifests, safe-path validation, and a
  deterministic per-case JSON report. Its seven focused tests pass. The
  canonical paired reference Metal/WGPU report is now promoted; the expanded
  state report remains a separate release artifact.
- Adapter-backed mesh compute now has opt-in asynchronous timestamp recovery
  under the existing `SOTF_GPU_TIMESTAMPS=1` policy. `MeshCompute` exposes
  timing-enabled, completed-sample-count, and last-duration diagnostics; the
  differential suite includes a timestamp regression that runs when a capable
  adapter is present. This proves the instrumentation contract, while
  production Metal timing still needs reference-adapter evidence.
- The six WGPU visual cases now emit canonical `comparison_id` values shared
  by the cross-adapter comparator, and the manifest validator requires those
  IDs. The checked-in baseline and QA fixtures were updated without changing
  image checksums.
- The native Python MeshPlot rendered-session matrix now exits cleanly on the
  no-Metal-device host: the focused `native_mesh_plot_tests` target passes
  10/10 and the full `gpui-python-showcase` binary passes 48/48, with six
  Metal-dependent tests reporting explicit developer skips. This isolates the
  remaining gap to rendered evidence on a compatible reference host rather
  than an aborting local harness.
- The deterministic offscreen fallback now rejects out-of-range retained
  `MeshUpload` indices by skipping the affected primitive instead of
  substituting the origin and drawing false geometry. The focused
  `mesh::gpu::offscreen` suite passes 7/7; adapter-backed and reference-host
  framebuffer parity remain separate release gates. The full
  `gpui-d3rs --features gpu-3d --lib` suite passes 1,021/1,021.
- Retained Python MeshPlot dirty-domain fingerprints now include the
  missing-value policy with field data, plus contour configuration,
  equal-aspect mode, axes, and interaction capabilities with style. The new
  regression exercises each supported option and confirms that field changes
  do not dirty geometry or camera; the focused cache suite passes 25/25 and
  the full `gpui-python-runtime --lib` suite passes 129/129. Formatting and
  `git diff --check` also pass. Reference-machine upload/memory evidence is
  still a separate release gate.
- The performance evidence contract now requires the named MeshPlot workload
  matrix in clean release manifests, not merely the presence of the two
  benchmark binaries. It covers 100k/200k preparation, 12-level isolines and
  filled bands, 200k BVH construction, full and partial 64-segment revolve
  with field mapping, drag-time LOD proxy/full upload and transition work,
  200k MeshPlot build and fit, 100k retained field replacement and camera
  frames, and surface/revolved retained picking. Developer manifests may
  still report partial local runs; `--require-clean` enforces every workload.
- Added `mesh_plot_fit_200000_triangles` to the `gpui-px` Criterion benchmark
  and registered its group with the performance collector. A focused local
  run completed the new fit benchmark and the existing 200k build, retained
  100k field/camera, and picking cases. The refreshed local current artifact
  contains 29 records and passes the strict workload contract when used as a
  complete artifact pair. The checked-in baseline remains stale and strict
  validation correctly rejects it for missing 200k/revolve/LOD records; this
  is still reference-machine release evidence, not a release pass.
- Focused QA verification passes: 24 Python performance/release-evidence
  tests, `cargo check --offline -p gpui-px --bench mesh_plot_frames
  --features gpu-3d`, `cargo fmt --all -- --check`, and `git diff --check`.
- The complete QA-script discovery now passes 58 tests (`test_qa_*.py`).
- Strict release validation also rejects performance artifacts explicitly
  generated from a dirty worktree, while allowing developer-mode partial
  diagnostics and baseline/current comparisons from different clean commits.
- Strict performance provenance now fails closed when `source_dirty` is
  missing as well as when it is true; clean evidence must explicitly declare
  `source_dirty: false`. Current-worktree verification passes 93 MeshPlot
  library tests and the complete `gpui-python-runtime --features showcase`
  gate (225 tests across library, host, showcase, and integration targets).
- Adapter-backed compute now records the selected `wgpu::Backend` for
  platform QA. The macOS differential suite passes 9/9 and confirms the
  adapter path reports Metal when a Metal adapter is available. This proves
  backend provenance and adapter selection; direct Metal-compute output
  parity and production timing remain separate release gates.
- Adapter-backed WGPU QA found and fixed a real retained-update failure: the
  2D custom draw re-entered its state `RefCell` during telemetry publication
  on cell-field updates. The draw now keeps state/resource borrows in one
  non-reentrant scope without cloning retained uploads. New geometry scalar
  buffers are initialized at their current revision on the 3D path, so only
  later field-only queue writes count as updates. The readback test now also
  releases its telemetry snapshot before the final frame. WGPU embedded
  viewport QA passes 8/8, cache-contract QA 10/10, GPU unit tests 12/12, and
  `cargo check --offline -p gpui-d3rs --features gpu-3d` passes.
- The WGPU visual wrapper was rerun on the current host and correctly produced
  an explicit skipped manifest: no usable Metal/Vulkan adapter is available.
  Embedded WGPU tests now report developer skips through a shared helper and
  honor `QA_WGPU_REQUIRED=1`; normal developer execution is allowed to skip,
  while required execution fails five adapter-dependent cases as intended.
  Therefore the earlier 8/8 test count is not current adapter evidence and no
  WGPU baseline was promoted.
- The full `gpui-d3rs --features gpu-3d --lib` suite passes 1,020/1,020 after
  the adapter-test skip/required-mode changes. `QA_WGPU_REQUIRED=1` was also
  exercised and fails with the expected unavailable-adapter diagnostic.

- The last completed focused native Metal MeshPlot runtime suite passed 8/8.
  The field update telemetry case captures the patched frame before reading
  adapter telemetry and verifies a scalar-buffer write with no geometry
  upload. A fresh rerun on the current host now exits status 1 before the
  first assertion, matching the native Python rendered lane's host-runtime
  failure; no MeshPlot assertion failure was reported.
- The native Python showcase test binary currently lists 47 tests. The
  non-rendering and resource/snapshot subsets are green, but the two focused
  rendered Metal cases
  (`native_metal_python_resource_plot_renders_and_emits_typed_selection` and
  `native_metal_invalid_resource_patch_keeps_the_last_valid_meshplot_frame`)
  both terminate the process with status 1 before the harness reports a Rust
  assertion. The full native process remains a release gate until this
  host-level teardown/runtime failure is diagnosed or reproduced on a clean
  reference host.
- Native Metal headless construction now has a fallible `try_new` path. The
  8-case `mesh_plot_native` suite and the 10-case native Python MeshPlot
  subset now complete cleanly on this host and report explicit developer
  skips when no compatible Metal device is available; this improves failure
  isolation but does not provide rendered Metal evidence.
- The macOS component-lab visual-capture factory now uses the same fallible
  renderer path, so missing Metal hardware is surfaced as capture failure
  data rather than a process exit; `cargo check -p gpui-component-lab
  --features visual-capture` passes.
- Field/style declarative rebuilds now retain the existing Metal custom draw
  and custom ID; geometry changes still replace the renderer/resource owner.
  This prevents field-only updates from re-uploading geometry.
- 3D PNG export now uses live plot margins and inner-viewport dimensions and
  composites the rendered plot into the full output image. Native/export
  parity now compares the shared plot viewport; exact cross-adapter pixel
  parity remains open.
- The Python host shutdown regression now verifies the complete handshake: the
  child receives shutdown, writes its marker, and is reaped by the host.
- The embedded headless-WGPU viewport suite now verifies adapter-backed field
  updates: a same-layout 3D update writes the scalar buffer without geometry
  re-upload; a vertex-to-cell layout change performs exactly one geometry
  resource rebuild; same-layout 2D and cell-field updates also avoid geometry
  upload and write the expanded triangle-local scalar payload. A 60-frame
  camera-only regression also preserves geometry and scalar-buffer storage.
- Native host shutdown cleanup is now factored into an idempotent
  `shutdown_runtime_state` path. Its focused regression verifies profiler
  cancellation, active MeshPlot resource release, frame-store clearing,
  retained MeshPlot cache/state clearing, and safe repeated cleanup; the full
  child-process/native recovery matrix remains open.
- Added a non-rendering native host recovery regression for malformed inline
  MeshPlot field patches. Invalid field data preserves the committed app,
  resource references, and revision/generation watermarks; a later valid
  patch at the same revision commits and clears the localized diagnostic.
- Runtime `SetMeshPlotProp` patches now accept the validated `axes` and
  `missing_value_policy` properties, covering labels, units, explicit ranges,
  grid visibility, and NaN-mask policy alongside the existing camera/viewport
  patch surface. View-specific `revolve` settings are now transactional too.
- The aggregate coverage gate now passes at `90,825 / 123,568 = 73.50%`, the
  required floor. The per-crate floors also pass (`gpui-px` 85.87%,
  `gpui-python-runtime` 90.49%).
- QA scripts now resolve Cargo/Rust tools from `~/.cargo/bin` when a restricted
  shell omits them from `PATH`; this makes metadata and benchmark evidence
  reproducible in the current environment.
- The Rust host patch transaction is now isolated from the GPUI drain loop and
  has a non-rendering regression covering a valid field-generation replacement,
  reference ownership updates, missing-resource rejection, stale-revision
  rejection, and preservation of the committed newest-valid app state.
- Native snapshot commit is now isolated behind the same host boundary. Its
  regression covers available-resource replacement, reference ownership
  changes, invalid-snapshot preservation, missing-frame arrival, and keeping
  the same-ID last-valid fallback cache.
- The MeshPlot accessibility summary no longer repeats the field description;
  a regression now requires one field announcement while retaining value
  range, selection, and control metadata. Manual VoiceOver/Narrator execution
  remains a release gate.
- The accessibility-tree regression now covers the toolbar region role and
  accessible name, the visible planar control order, omission of the 3D view
  menu for planar plots, and the disabled/export state. The focused target
  passes 3/3; manual VoiceOver/Narrator and live keyboard QA remain open.
- Shared UI-kit menus now register named menu regions and named menu-item or
  checkbox nodes, including disabled and checked states; MeshPlot mode/view
  menus provide explicit accessible labels. The focused UI-kit menu/context
  menu integration lane passes 21 tests. Manual screen-reader QA remains open.
- Python host reload now resets revision and MeshPlot generation history along
  with retained resources. A restarted child can therefore begin at revision
  and generation one without being rejected as stale; focused session and
  native-qa reset/recovery tests pass.
- Successful snapshot/patch pruning now retains generation history only for
  live MeshPlots. Removing a plot releases its old generation history while
  preserving late-result rejection for active plots; core and host recovery
  regressions pass.
- The accessibility description now also announces configured axis labels and
  units. Its focused contract covers view kind, counts, field range, selected
  stable IDs/value, wireframe state, and disabled interaction controls; the
  two-test accessibility target passes. Manual VoiceOver/Narrator execution
  remains a release gate.
- Successful native snapshots now clear stale snapshot-scoped global and
  per-plot diagnostics, so recovery from a malformed snapshot does not leave
  an obsolete error overlay after the next valid snapshot.
- The native 3D export/parity regression now also checks a camera-only frame:
  geometry upload and scalar-write counters must remain unchanged while the
  retained camera moves. The current host cannot revalidate this adapter lane
  because even the unchanged control Metal test exits during macOS teardown.
- Corrected native MeshFrames now clear only the matching resource-generation
  diagnostic, so malformed-then-corrected frame recovery removes the stale
  plot overlay without hiding unrelated errors.
- Native MeshPlot resource-error routing now matches both resource ID and
  generation. An unknown generation can no longer contaminate a plot that
  owns another generation; the focused native resource/snapshot suite passes
  8/8, including the new regression, pre-frame diagnostic localization, and
  malformed-then-corrected recovery.
- Added an end-to-end native Metal regression for malformed binary MeshFrames:
  a rejected generation preserves the rendered last-valid frame, while a
  corrected generation updates the field and becomes the retained valid
  specification. The test compiles, but execution is currently blocked by
  the host-wide macOS Text Input Services teardown abort.
- Added a host-independent retained-contour regression proving duplicate
  preparation is suppressed, a newer field revision invalidates the older
  worker key, stale results cannot commit, and the newer preparation can still
  complete. The focused `gpui-px` test passes.
- MeshFrameStore now distinguishes transient ingest failures from explicit
  release/eviction staleness. Multi-chunk assembled-payload mismatches roll
  back their poisoned assembly, and budget-pressure failures can be retried
  at the same generation after capacity is released. The 13-test mesh-frame
  suite passes, including both recovery regressions.
- Native resource diagnostics now also resolve against the declarative
  snapshot when binary frames have not arrived yet. A malformed pre-frame
  generation is localized to the declared plot, and the corrected frame clears
  that diagnostic; the focused native resource/snapshot suite passes 8/8.
- The retained `Mesh3DRenderer` facade now rejects lower field revisions
  before dispatching to a backend, preventing late worker results from
  overwriting visible scalar values or issuing another adapter write. The
  retained geometry path now applies the same lower-revision guard before
  upload, preventing late geometry workers from replacing newer retained
  buffers. The cache-contract suite passes 10/10 with both stale-write
  regressions.
- The broader offline `gpui-d3rs --features gpu-3d --lib` suite passes
  1,020/1,020 after the revision guard.
- MeshPlot runtime pruning and reset/selection handling now preserve the
  distinction between a declarative UI node ID (the patch address) and the
  MeshPlot spec ID (the retained GPU/cache owner), so Python's supported
  `ui.mesh_plot(..., id=...)` override cannot orphan retained state; localized
  patch errors are recorded under both aliases so the visible plot can render
  the diagnostic while retaining its last-valid frame.
- Binary MeshFrame ingestion is now isolated behind the same host boundary;
  malformed assembled payloads are rejected without entering the store and
  produce a plot-local diagnostic carrying the resource generation and patch
  correlation.
- The current non-rendering showcase host and showcase binaries each pass 37
  tests, the focused Python subprocess/session suite passes 5 tests, and the
  complete Python unittest suite passes 180 tests after these host changes.
- The complete `gpui-python-runtime --features showcase` package test gate
  passes 221 tests serially (143 library, 37 host, 37 showcase, 4
  integration); native Metal rendering remains a separate adapter-backed
  lane.
- Python MeshPlot specs now accept a validated optional `axes` object with
  labels, unit, explicit x/y ranges, and grid visibility. Native conversion
  preserves equal/fill aspect and view-specific default labels when only
  partial axes configuration is supplied. Focused IR/native axes tests pass;
  the full showcase-feature and Python discovery gates below include this
  parity.
- Python interaction declarations are now consumed by native conversion:
  omitted interaction configuration retains the interactive default, an
  explicit empty list disables interaction, and validated names map to a
  custom native capability set. Pointer, keyboard, and toolbar paths gate
  pan, zoom, inspect, select, reset, and fit consistently.
- Binary MeshFrame ingestion is now isolated behind the same host boundary;
  malformed assembled payloads are rejected without entering the store and
  produce a plot-local diagnostic carrying the resource generation and patch
  correlation.


**Date:** 2026-08-16
**Source:** `20260808-plot.md` and `20260810-plot.md` (the requested literal
names `202060808-plot.md` and `202060810-plot.md` are not present in this
checkout)
**Reviewed branch:** `feature/mesh-plot` at `7e3a409`; the worktree is
currently dirty with an uncommitted WGPU visual-matrix expansion.
**Implementation worktree:** `20260816-plot`

This task list reflects the implementation review of `feature/mesh-plot`.
It distinguishes the substantial work already present from the evidence and
follow-up work still required before product integration.

## Status legend and implementation summary

- `[x]` Fully implemented and covered by the currently available evidence.
- `[~]` Partially implemented: code or focused tests exist, but an acceptance,
  integration, hardware, visual, accessibility, or release-evidence gap
  remains.
- `[ ]` Not complete: the work or its required proof is still outstanding.

### Fully implemented

- Core mesh data/validation and the covered contour, isoline, mask, winding,
  shared-edge, saddle, and degenerate-triangle algorithms.
- Revolved-mesh CPU preparation for the covered sweep, caps, normals, scalar
  replication, orientation, and degenerate-triangle cases.
- Equal-aspect viewport mapping across geometry, input, clipping, axes, GPU
  projection, and deterministic export.
- Retained CPU geometry/BVH reuse, field-only update ordering, deterministic
  SVG/PNG export, and the focused zero-allocation picking/update contracts.
- The covered native Metal 3D/revolve route, including depth-tested surface
  rendering, clipping, wireframe differences, resize/equal-aspect behavior,
  and native framebuffer/export parity for the serialized Surface3d and
  revolve cases.
- The 99-case Metal component-lab capture completed all 99 captures, but the
  current baseline diff is 72/99 zero-pixel passes. The 27 failures are the
  nine picking, nine revolve, and nine Surface3D variants whose checked-in
  baselines predate the selected overlay, revolved-cylinder, filled-surface,
  and orientation-triad behavior now present in the captures.
- Cleanup of the temporary native-parity diagnostic PNG captures.

### Partially implemented

- Cross-adapter 3D/revolve rendering and parity: the native Metal route is
  covered by serialized framebuffer tests, while embedded WGPU has focused
  viewport/readback coverage but no promoted reference-adapter visual lane.
  Retained GPU dirty domains, compute/fallback behavior, Python/native
  session/resource parity, host ownership, invalid-input fallback,
  interaction behavior, and cache lifecycle likewise have meaningful
  implementation and focused tests, but not complete end-to-end or
  reference-machine evidence.
- Export parity beyond the covered Metal cases: deterministic SVG/PNG and
  native Metal framebuffer/export parity are covered; exact cross-adapter
  pixel parity remains open.
- Theme/layout coverage: the registered Metal matrix is captured locally, but
  clean promotion and cross-adapter comparison are outstanding.

### Not complete

- Exact cross-adapter pixel parity, Metal-compute parity/timing, long-run
  reference-adapter upload/memory/cache evidence, full accessibility QA,
  color-vision-deficiency QA, and live Sonium GPUI-window sign-off.

## Current status

The feature is a substantial implementation prototype. The core data model,
2D unstructured contours and isolines, validation, retained rendering paths,
native interaction, Python resource contracts, deterministic SVG, focused
regression tests, retained field-update ordering, last-valid-frame fallback,
and preparation/picking timing counters are present. WGPU retained 3D now has
an opt-in asynchronous render-pass timestamp recorder for adapter GPU
duration and a retained-generation memory-bounded regression. The Python
child-process
session now also exercises field and geometry replacement, camera/viewport/
selection patches and resets, request correlation, and a future schema
version patch. Native host ownership coverage now exercises a bounded store,
explicit-release rejection while a plot is active, budget-pressure failure,
and successful release after plot ownership is removed.

Release readiness still has Metal-compute parity/timing, long-run
resource-cache evidence, complete accessibility QA, color-vision-deficiency
review, live Sonium sign-off, and final clean-release binding open. The
reference-host six-scene Metal/WGPU comparison now passes exactly after the
Metal vertex ABI, orientation-triad NDC, and isoline derivative-stability
fixes. The current worktree includes the promoted six-scene WGPU baseline and
the passing paired report; strict clean-release rerun remains outstanding
because this worktree is dirty.

The latest retained-GPU refactor separates 3D scalar values from retained
position/normal geometry. Compile and cache-contract checks pass, including the
field-only update path. The Metal shader now gates scalar reads on the active
field flag, and the shader/cache regression covers both enabled and disabled
paths. The serialized native Metal runtime suite passes 8/8 with real adapter
access; its field-update telemetry case confirms a scalar-buffer write without
a geometry upload. Long-run reference-adapter memory and cache-eviction
evidence remain open.

The latest native parity investigation is resolved. The serialized
`native_metal_live_3d_state_exports_deterministic_surface_and_revolve_artifacts`
case now captures the expected 3D colored surface/revolve output and passes
its framebuffer/export parity assertions. Native Metal and WGPU adapter-backed
evidence now pass, and the six canonical cross-adapter scenes compare exactly
with zero failed cases. The remaining parity work is expanded selection/axes
coverage and release artifact binding, not a known native Metal assertion
regression. Camera, displayed-range, and NaN-mask state now have a separate
exact three-case reference-host report.
The temporary
`/private/tmp/mesh-native-*.png` diagnostic captures used during the
investigation have been removed.

The isolated Metal component-lab run now covers all 99 registered MeshPlot
cases. The current diff has 72/99 zero-pixel passes and 27 failures, limited
to the picking, revolve, and Surface3D variants. The failures match newly
implemented visuals against stale checked-in baselines; baseline promotion is
guarded and must wait for a clean source-bound revision. The archive remains
structurally validated, but it is not current zero-diff release evidence.

The source review contains inconsistent aggregate counts between its executive
summary and detailed acceptance table. This list follows the detailed status
and the remaining-task evidence rather than treating the feature as complete.

## Latest continuation evidence — 2026-08-17

- [x] Add a dedicated native Metal six-scene capture example using the same
  canonical IDs and 256×192 output contract as the WGPU lane. The `metal-qa`
  Cargo feature supplies GPUI/macOS test support; the example compiles
  offline, and an adapter-backed host run produced six PNGs plus a validated
  `metal-headless` manifest.
- [x] Add `scripts/qa_mesh_metal_visual.sh` with developer-skip and required
  release modes; wire it into `Justfile` before the cross-adapter lane and
  release-evidence validation. The manifest validator now accepts an explicit
  renderer while preserving the WGPU default. Focused manifest and release-
  evidence tests pass (26 tests, one platform-dependent skip).
- [x] Align the Metal capture fixture with WGPU's canonical retained 3D path
  and make Metal 3D uniform handling consume a supplied `Camera3D` for the
  mesh view-projection matrix as well as the orientation triad. The complete
  serialized native Metal regression remains green at 8/8.
- [x] Normalize paired framebuffer alpha to an opaque-black WGPU clear, which
  removes the capture-contract-only difference; the required six-case WGPU
  visual baseline passes.
- [x] Produce exact six-scene Metal/WGPU parity. A reference-host rerun now
  compares all six canonical cases with zero changed pixels and zero failed
  cases; no comparator threshold was relaxed.
- [x] Resolve the remaining canonical scene/camera/shader/viewport differences:
  the Metal vertex ABI now has a compile-time 48-byte stride assertion, the
  orientation triad uses the shared NDC Y convention, and both shaders
  quantize isoline derivative width identically for stable antialiasing.

### Verification refresh — 2026-08-17 (Metal vertex-buffer ABI and parity)

- [x] Diagnosed the remaining low-level Metal capture symptom: the canonical
  Metal capture showed only malformed orientation-triad lines because the MSL
  `VertexIn` stride was 32 bytes while Rust's `MetalVertex` upload was 48
  bytes. The MSL input now declares the scalar slot and three scalar padding
  members, matching position (16) + normal (16) + value (4) + padding (12).
- [x] `cargo check --offline -p gpui-d3rs --example
  mesh_metal_visual_capture --features metal-qa` passes, and the extracted
  MSL passes the installed Apple Metal compiler syntax check.
- [x] On the compatible reference host, regenerated the six Metal PNGs and
  manifest after the ABI fix; the orientation triad initially exposed a
  vertical NDC sign mismatch, which was corrected in the Metal backend.
- [x] Quantized the WGSL/MSL isoline derivative width on the same 1/64 phase
  grid. The WGPU capture and promoted `isoline.png` baseline now agree, and
  the paired comparator reports six passed cases with zero changed pixels.
- [x] `scripts/qa_mesh_wgpu_visual.sh` passes 6/6,
  `scripts/qa_mesh_cross_adapter_visual.sh` passes 6/6 and validates the
  release report, and developer `qa_release_evidence.py` generation passes.
- [x] Required-mode `QA_METAL_REQUIRED=1`, `QA_WGPU_REQUIRED=1`, and
  `QA_CROSS_ADAPTER_REQUIRED=1` reruns all pass on the reference host, proving
  the release lanes do not silently accept skipped adapter evidence.
- [x] Adapter-backed `mesh_wgpu_embedded_viewport` passes 14/14, native Metal
  `mesh_plot_native` passes 8/8, the full offline `gpui-d3rs` library suite
  passes 1,021/1,021, and the focused `gpui-px` MeshPlot slice passes 93/93.
- [x] WGPU offscreen export now exposes an explicit-camera entry point while
  retaining the default-camera API; its adapter-backed regression proves a
  camera changes the projected frame without mutating retained scene state.
- [x] The same adapter-backed slice now verifies displayed-range changes keep
  geometry pixels stable while changing scalar colors, and NaN masking removes
  only the affected surface region; native selection/axes/export contracts
  remain covered by the existing `gpui-px` suite.
- [x] `gpui-python-runtime --features showcase` passes 232 tests across its
  library, host, showcase, and integration targets; the supported Python 3.11
  QA-script suite passes 62/62.
- [~] Strict clean-release evidence still requires the implementation worktree
  to be committed and the broader camera/range/selection/mask cross-adapter
  matrix plus manual UX gates to be completed.

## Features

### P0 — required before product integration

- [x] Complete canonical cross-adapter native Metal and embedded WGPU 3D/revolve
  parity. The reference-host six-scene report passes exactly; camera,
  displayed-range, and NaN-mask state also have an exact three-case expanded
  report. Expanded selection/axes captures remain separate.
  - Verify HiDPI and resize behavior, depth testing, clipping, lighting,
    wireframe, orientation triad, high-contrast rendering, and offset or
    partially clipped charts. Native Metal and embedded WGPU tests now cover
    depth-tested surface/revolve frames, clipping, resize, HiDPI, offsets, and
    wireframe differences.
  - [x] Diagnose and fix the native 3D test routing/Metal vertex ABI issue
    that produced a planar-looking live `Surface3d` frame. Serialized native
    Surface3d and revolve framebuffer/export parity now passes.
  - [x] Remove the temporary native parity PNG saves after diagnosis; the
    corresponding `/private/tmp/mesh-native-*.png` captures are absent.
  - [~] Add stable native visual output and cross-adapter baselines; the
    committed 99-case Metal archive and six-case WGPU baseline pass their
    visual validation, and the reference-host cross-adapter report passes all
    six canonical cases exactly. Clean-commit release binding remains open.
    The release validator checks dimensions, safe paths, checksums, and
    opaque-pixel counts and rejects skipped evidence in clean-release mode.
    `scripts/mesh_plot_visual_compare.py` now provides reusable exact or
    tolerance-defined per-pixel comparison and JSON reporting. The expanded
    camera/range/mask report is implemented and validated; expanded selection
    and axes captures remain outstanding.

- [~] Complete retained dirty-domain handling.
  - Prove that field/style updates preserve geometry, topology, normals,
    BVHs, camera, viewport, and selection.
  - Prove that geometry and revolve-view changes invalidate exactly the
    necessary retained resources.
  - CPU preparation counters and retained GPU field-write counts/bytes now
    exist. Adapter-backed GPU geometry/field-write counters, field capacity,
    and approximate resident buffer/depth bytes are now exposed for WGPU and
    Metal custom draws. The WGPU 3D path now keeps positions/normals and
    scalar values in separate buffers, so same-layout field updates queue-write
    only the scalar buffer; a vertex/cell field-layout change rebuilds the
    retained resource. The Metal field-enabled shader guard is implemented and
    covered by the cache-contract regression, and the focused native runtime
    telemetry assertion now verifies the no-geometry-upload contract. Both
    adapter backends now also accumulate geometry-allocation, scalar-write,
    and retained-frame submission timings through `MeshSceneState` and expose
    them via `RetainedMesh3DStats`. Production reference-machine capture and
    long-run cache evidence remain open.

- [~] Finish GPU compute integration.
  - Complete capability-based adapter selection and CPU fallback reporting.
  - Add Metal-compute parity where supported.
  - `MeshComputeBackend` now exposes available and last-used backend
    reporting, including explicit CPU-reference fallback. A public
    `MeshCompute::cpu_reference()` constructor makes fallback behavior
    deterministic to test even on adapter-equipped machines. The compute
    unit suite passes 13/13, and the adapter/CPU differential integration suite
    passes 8/8 for field ranges, isolines, filled bands, projection, masks,
    tie-breaks, and finite output. The macOS provenance regression now passes
    9/9 and reports Metal on the adapter-backed path; direct Metal-compute
    parity and GPU execution timing evidence remain open. Adapter submission
    timing is now recorded for geometry allocation, scalar writes, and retained
    frames and is asserted on both the Metal and WGPU lanes.

- [~] Complete Python and resource-backed patch parity.
  - Cover inline and retained resources, masks, stable IDs, field-only
    updates, geometry replacement, camera/viewport patches, selection, stale
    revisions, stale resources, and future schema versions.
  - Verify that invalid patches retain the newest valid rendered frame.
  - Rust/Python unit coverage now verifies generation monotonicity, explicit
    drop/clear behavior, stale retransmission rejection, and last-valid
    resource fallback. The native showcase now retains each plot's decoded
    resource generations across eviction, replacement, and explicit drop, and
    releases them on plot replacement, snapshot cleanup, and host drop. A real
    child-process showcase-session test now covers
    initialize/ready negotiation, MeshPlot snapshot emission, event handling,
    field and geometry replacement, camera/viewport/selection patches and
    resets, request correlation, generation, shutdown, and a future
    schema-version patch. A subprocess regression now covers both
    inline-geometry/resource-field and resource-geometry/inline-field
    declarations. The Python session loop now reports malformed JSON or
    non-object messages and continues serving; a recovery/shutdown subprocess
    regression covers those cases. The native stdout reader now also keeps a
    line-synchronized session alive after a malformed patch and forwards the
    error before later heartbeats or patches. Direct native shutdown cleanup is
    now covered by an idempotent host-state regression; full stale-recovery and
    native host-session shutdown/recovery matrix coverage remains open.
    Protocol-level stale patch rejection is now exercised without dropping
    subsequent messages. Shared `DropResource` routing now releases audio-only
    resources without producing a false MeshPlot error and reports an error
    only when neither runtime store owns the requested generation.
    The showcase now uses the shared `native_mesh_plot` decoder and builder
    directly; the duplicate local decoder and unreachable builder/camera
    stacks were removed. Inline and retained geometry/field inputs now share
    finite-value validation, while NaN field samples remain available for
    `MaskNaN`.
    Python declarations now also carry optional validated axes labels, unit,
    x/y ranges, and grid visibility into the native `Axes2d` configuration;
    omitted labels retain planar or axisymmetric defaults.
    The generic MeshPlot property patch path now accepts `axes`,
    `missing_value_policy`, and `revolve`, validating updated ranges/labels,
    mask policy, and view-specific revolve settings transactionally.
    Interaction lists are now passed through native options, with explicit
    empty-list disable and per-action capability filtering in pointer,
    keyboard, and toolbar paths.
    Native camera parsing now completes before any retained-state mutation, so
    an invalid 3D camera patch cannot partially change style, viewport,
    selection, or camera state before falling back to the last-valid plot.
    Native build failures now restore a lightweight configuration snapshot,
    covering decoded field-length, topology, and revolve validation failures
    without cloning retained geometry, BVHs, or GPU buffers.
    Reloading a Python child also resets session revision/generation history
    so a new producer can safely restart its counters.

- [~] Finish native framebuffer export parity.
  - Add native framebuffer readback where supported.
  - Compare native framebuffer output with deterministic SVG and PNG output.
  - Deterministic current-camera SVG and caller-scale PNG exports preserve
  live state without mutation. Serialized native Metal framebuffer/export
  readback and parity now pass. 3D and revolved SVG now emit the configured
  colorbar from the displayed color range; `Colorbar::to_svg()` exposes that
  range through `data-lower` and `data-upper`. Configured `Axes2d` labels and
  units are also preserved in 2D SVG. Exact cross-adapter pixel parity
  remains open.

- [~] Complete resource-cache lifecycle behavior.
  - Byte budgets, statistics, reference counts, deterministic eviction,
    explicit drops, clear behavior, and generation history are covered by
    focused Rust/Python tests. Native host ownership is wired through
    MeshPlot render, snapshot, patch, and drop paths. The focused native
    host-ownership regression now proves that active plot references block
    explicit release, that a bounded 204-byte store rejects budget pressure
    without evicting active resources, and that all resources can be released
    after plot ownership is removed. Native and Python regressions now run
    1,000 alternating field generations while retaining geometry and assert
  bounded bytes/entries plus complete release; adapter telemetry now exposes
  GPU-memory counters. The adapter-backed WGPU regression now performs 1,000
  field writes on a connected 32×32 grid and proves one geometry upload,
  stable field capacity/resident bytes, and exact scalar-write accounting;
    reference-host capture and eviction evidence are still required. Focused
    2D/3D adapter tests also verify same-layout cell-field replacement writes
    expanded values without another geometry resource.

### P1 — required for specification completeness

- [~] Complete interaction behavior and runtime focus QA.
  - Verify 2D pan, zoom, box zoom, fit, reset, hover, click selection, and
    keyboard equivalents.
  - Verify 3D orbit, pan, zoom, fit-to-bounds, standard views, projection
    switching, hover inspection, and selection.
  - Python interaction capability lists now gate native pointer, keyboard,
    and toolbar actions individually; focused capability and keyboard tests
    pass. Product-level runtime, cross-adapter, and screen-reader QA remains
    open.
  - Native tests cover pointer selection, keyboard zoom/pan, equal-aspect
    resize, and selection persistence. Toolbar menus now receive a dedicated
    focus handle, start on their first item, support the existing
    arrow/Enter/Escape menu contract, and return focus to the plot surface on
    selection or close. Menu regions and items now have explicit accessibility
    names/states; product-level runtime and screen-reader QA remains open.

- [~] Complete last-valid-frame behavior.
  - Add explicit rendered tests for malformed field patches, invalid geometry
    resources, invalid masks, stale generations, and evicted resources.
  - Native invalid-resource tests prove newest-valid-frame fallback and state
    ownership preservation, including malformed field and mask resources.
    Native option parsing now also rejects malformed 3D camera values before
    mutating the retained state owner; a focused regression covers the full
    style, viewport, selection, and camera state snapshot.
    Malformed inline field patches now have a non-rendering recovery
    regression: the newest valid app and resource ownership remain committed,
    and a later valid patch can recover at the same revision. Full Python-
    session recovery coverage remains open.
    A second regression covers a decoded field-length failure and verifies the
    retained configuration is restored after `MeshPlot::build` rejects it.

- [~] Complete planar and revolved picking evidence.
  - Retain the planar and revolved BVHs and derived fields across field/style
    updates.
  - Retained BVH reuse, external IDs, zero-allocation 1,000-pick contracts,
    and representative benchmark timings are covered. Broader product
    interaction evidence remains open.

- [x] Verify equal-aspect behavior end to end.
  - Keep geometry, axes, grid, clipping, hover, click selection, wheel zoom,
    SVG export, and GPU projection on the same visible letterboxed viewport.
  - Covered by the inverse-mapping test and serialized native tests for
    nested offsets, resize, visible-viewport input, and letterbox-bar
    rejection.

## Testing

- [x] Maintain algorithm regression coverage for single triangles, shared
  edges, holes, disconnected islands, saddle fields, constant fields, masked
  triangles, exact-on-level vertices and edges, reversed winding, and linear
  fields with analytically known isolines.

- [x] Maintain shared-edge crack checks for every contour level and ensure
  deterministic results independent of equivalent triangle input order.

- [x] Cover full and partial revolved sweeps, axis vertices, end caps, scalar
  replication, normals, source orientation, and degenerate-triangle removal.

- [~] Add native Metal and WGPU rendered-frame tests for mesh-only, smooth
  vertex fill, flat cell fill, filled contours, isolines, combined mode,
  axisymmetric section, revolve, and 3D surface stories. Native Metal and
  embedded WGPU frame tests exist, including adapter-backed offscreen WGPU
  readback with scalar-color assertions, but the complete mode/theme matrix
  is not yet covered. The WGPU manifest validator has focused tests for
  missing cases, checksum mismatch, missing baseline, and developer-skip
  versus release-required behavior. The cross-adapter comparator has focused
  exact-match, tolerance-boundary, dimension, case-set, and malformed-input
  tests; the canonical paired report and the expanded camera/range/mask report
  pass on the reference host, while expanded selection/axes captures remain
  open.

- [~] Add full Python subprocess/session tests instead of relying only on
  Python unit tests and Rust cache tests. The showcase-session subprocess
  path now covers retained patch and reset operations, field/geometry
  replacement, inline/resource cross-products, request correlation, and
  future-schema handling, malformed-message recovery, heartbeat continuity,
  and shutdown callback execution. Native malformed-patch reader recovery and
  stale-revision rejection are covered by Rust reader regressions, and the
  native host patch transaction now covers valid replacement, ownership
  updates, missing-resource rejection, and preservation of the newest valid
  committed state without a GPU window. An integrated non-rendering recovery
  matrix now covers valid replacement, snapshot-before-frame recovery,
    malformed/corrected frames, invalid patches, active-resource drop rejection,
    plot removal, and a late superseded-generation frame after recovery. The
    native resource/frame protocol matrix still needs
  end-to-end rendered cases. The shared native
  `DropResource` release router is covered for audio-only, mesh-only, unknown
  generations, and shared generations retained by multiple active plots. The
  stdout reader now has a binary audio-frame/drop-resource ordering regression
  as well.

- [~] Add a native host-session lifecycle regression for an active plot:
  retain all geometry/field/mask handles, reject their eviction or explicit
  drop while rendered, keep the prior frame through an invalid patch, and
  release every handle when the plot is removed or the host shuts down. The
  focused ownership test passes, including DropResource-equivalent release
  rejection and budget-pressure retention. The direct shutdown regression now
  verifies profiler cancellation, frame-store clearing, and idempotent cleanup;
  the full native host-session recovery matrix remains open for rendered
  Metal cases.

- [~] Add export parity tests for current camera, displayed color range,
  contour levels, selection annotations, axes, colorbar, and orientation
  axes. Current-camera SVG/PNG determinism, state preservation, and serialized
  native Metal framebuffer/export parity are tested. The 3D/revolved SVG
  colorbar uses the displayed range and carries structured lower/upper range
  metadata, and 2D SVG uses configured axis labels and units; exact
  cross-adapter pixel parity remains open.

- [~] Keep allocation and retained-state regressions for navigation, field
  replacement, 1,000 updates, 1,000 picks, LOD transitions, BVH reuse, and
  stale-result rejection. The retained 1,000-update/1,000-pick contracts
  pass; native Metal and embedded WGPU telemetry now record GPU
  geometry/field writes, field capacity, approximate resident bytes, and
  adapter submission timings, while reference-adapter memory, cache-eviction,
  and visual LOD evidence remain open.

- [x] Complete a green full-workspace test gate. The network-enabled serial
  `cargo test --workspace --lib --tests -- --test-threads=1` gate passes,
  including all three `gpui-scaffolder` compile-smoke tests that previously
  lacked the external `zed` dependency.

## QA and UX

- [~] Promote all 99 light/dark/high-contrast × dashboard/panel/mobile
  component-lab baselines from a clean source-bound release commit. All 99
  captures complete, but the current checked-in archive compares 72/99 with
  zero changed pixels; the 27 failures are stale baselines for picking,
  revolve, and Surface3D behavior. Do not promote until implementation is
  final and the clean-worktree guard permits regeneration.

- [x] Add a WGPU visual-baseline lane and compare it with the Metal rendering
  path. The adapter-backed six-scene lane, promoted baseline, and strict
  reference-host paired report all pass; expanded state/annotation coverage
  and clean-commit release binding remain open.

- [ ] Run VoiceOver and Narrator product QA for:
  - the graphic summary;
  - title, view, counts, field label, unit, range, and selection state;
  - toolbar controls and focus order;
  - keyboard pan, zoom, fit, reset, view changes, and selection traversal;
  - selection announcements and tooltip contents.

- [~] Validate light, dark, high-contrast, narrow/mobile, dashboard, and panel
  layouts for colorbar, mesh edges, isolines, masked regions, triad, focus
  rings, and selected annotations. Local component-lab captures cover the
  registered matrix; clean promotion and cross-adapter comparison remain
  open.

- [~] Verify color-scale interpretation for supported color-vision
  deficiencies and confirm that no result relies only on red-versus-green
  contrast. An automated protan/deutan/tritan simulation regression now
  checks adjacent samples for every named scale; manual visual CVD review
  remains outstanding.

- [~] Run live Sonium examples in a GPUI window for planar mesh, scalar fill,
  contours, axisymmetric section/revolve, and 3D surface inspection. Smoke
  exports now cover all five public-builder views, and
  `GPUI_MESHPLOT_WINDOW=1 cargo run -p gpui-px --example mesh_plot_showcase
  --features gpu-3d` opens an opt-in tabbed live viewer for those views. The
  mode compiles and reaches the application on this host, but live interaction
  and rendered inspection remain required for product sign-off.

- [x] Keep capability reports marked `partial` until focused tests, clean
  visual baselines, cross-adapter runtime evidence, accessibility QA, and
  reference performance evidence are all present. The report and its
  11-story provenance regression keep every MeshPlot capability at `partial`.

## Performance and resource evidence

- [~] Run release benchmarks on the reference desktop for:
  - initial display and fit at 100,000 vertices / 200,000 triangles;
  - replacement of 100,000 vertex values;
  - 12-level filled contours and isolines for 100,000 triangles;
  - sustained camera navigation after upload;
  - full and partial 64-segment revolve preparation;
  - repeated planar and 3D picking. A fresh controlled 10-sample diagnostic
    on this arm64 host measured 100k preparation at ~835 µs, 100k isolines at
    ~46.1 ms, 100k filled bands at ~30.8 ms, 200k preparation at ~1.60 ms,
    200k isolines at ~79.2 ms, 200k filled bands at ~64.5 ms, 100k BVH build
    at ~64.4 ms, 200k BVH build at ~141 ms, full 64-segment revolve at
    ~5.54 µs, and partial capped revolve at ~5.93 µs. The retained-frame
    diagnostic measured 100k-value field replacement at ~16.7 µs, 1M-value
    replacement at ~207 µs, surface BVH picking at ~1.32 µs, and revolved
    picking at ~662 ns. The clean M4 Pro run now records all 29 named MeshPlot
    workloads, including 200k contours, BVHs, 64-segment revolve/field
    mapping, retained camera/field frames, and planar/revolved picking;
    adapter submission timing is now captured by runtime telemetry, while
    Metal-compute execution timing and long-run memory/eviction evidence remain
    open. WGPU retained 3D has an opt-in asynchronous GPU timestamp lane.

- [x] Demonstrate 60 Hz camera navigation on the reference machine. The real
  WGPU adapter regression measures 60 camera-only frames and enforces a
  one-second average budget.

- [x] Demonstrate no per-frame geometry upload during camera-only frames. The
  adapter-backed WGPU test proves 60 camera-only frames preserve geometry
  bytes, scalar capacity, resident bytes, and upload/write counts while
  meeting the one-second budget.

- [~] Run 1,000 alternating field updates and record:
  - geometry revision and upload counts;
  - CPU and GPU bytes;
  - allocation counts;
  - cache size and eviction behavior;
  - field-buffer capacity and retained scene identity. Retained tests cover
    these CPU/scene invariants; the adapter-backed WGPU lane now covers 1,000
    connected-grid field writes with stable geometry, field capacity, resident
    bytes, and exact write totals. A 100-generation geometry replacement
    regression also keeps current resident bytes bounded. Reference-adapter
    cache-eviction and driver-private memory capture remain open.

- [~] Record production timings for mesh preparation, contour generation,
  revolve generation, BVH construction, hit testing, GPU uploads, field-only
  replacement, camera-only frames, and export encoding. CPU timing counters
  and benchmark records exist. Adapter geometry-allocation, scalar-write, and
    retained-frame submission timing are now recorded and asserted on real
    Metal/WGPU lanes. A deterministic Surface3D PNG benchmark now covers
    export encoding; WGPU asynchronous GPU execution timing is now available
    through the opt-in timestamp lane, while Metal-compute timing and
    resident-memory eviction remain open.

- [~] Measure drag-time LOD behavior on representative connected meshes,
  including proxy upload time, full-resolution restoration, visual quality,
  and picking correctness. The retained LOD regression now proves one proxy
  upload plus one full-resolution restoration, association-safe proxy field
  mapping, and full-upload restoration; representative timing, adapter-backed
  visual quality, and live picking evidence remain open. A connected
  100k-triangle Criterion slice records proxy upload preparation at ~7.6 µs,
  full restore preparation at ~306 µs, and proxy field mapping at ~5.7 ns;
  these are CPU preparation measurements, not adapter-backed frame or
  visual-quality proof.

- [x] Generate release evidence from a clean worktree and require
  `scripts/qa_release_evidence.py --require-clean` to pass. Final manifest
  source is clean on the final feature commit.

### Latest verification

- `cargo test --offline -p gpui-python-runtime --features showcase` — 221
  passed across the library (143), host (37), showcase (37), and integration
  tests (4); doc-tests completed with zero tests.
- `PYTHONPATH=crates/gpui-python-runtime/python python3.11 -m unittest
  discover -s crates/gpui-python-runtime/python/tests` — 180 passed.
- Focused Python MeshPlot tests — 11 passed; focused native MeshPlot options
  tests — 7 passed, including axes defaults/ranges/unit/grid behavior and
  interaction default/disable/preset mapping.
- `cargo test --offline -p gpui-px --features gpu-3d
  mesh_plot::interaction::tests --lib` — 21 passed, including capability-aware
  keyboard navigation; the public interaction-type test covers custom
  capability parsing and duplicate rejection.

- `cargo test --offline -p gpui-d3rs --features gpu-3d,gpu-compute --lib` —
  1,026 passed, including the 13/13 compute unit suite, explicit
  CPU-reference fallback, and backend-reporting regressions.
- `cargo test --offline -p gpui-d3rs --features gpu-3d,gpu-compute
  --test mesh_compute_diff_tests` — 8 passed; adapter and CPU-reference
  outputs agree for ranges, isolines, filled bands, projection, masks,
  tie-breaks, and finite output.
- `cargo check --offline -p gpui-px --features gpu-3d` — passed.
- `rustfmt --edition 2024 --check crates/gpui-d3rs/src/mesh/gpu/compute.rs`
  and `git diff --check` — passed.
- `cargo test --offline -p gpui-px --features gpu-3d --lib` — 527 passed,
  including per-action interaction gating and the stale-contour-worker
  regression.
- Focused export tests: 21 passed, including
  `surface_svg_includes_colorbar_for_the_displayed_value_range`; 3D and
  revolved SVG colorbars use the displayed range and emit `data-lower` /
  `data-upper` metadata, custom 2D axis titles/units, explicit axis ranges,
  and grid visibility are exported.
- MeshPlot module tests — 90 passed; `Axes2d` explicit ranges and grid
  visibility are validated and honored by live initial domains, accessibility
  ranges, and deterministic SVG export.
- `cargo test --offline -p gpui-px mesh_plot::interaction::tests --lib` — 10
  passed, covering dirty-domain reuse, stale field replacement, retained
  contours, and asynchronous preparation invalidation.
- `cargo test -p gpui-px --features native-qa --test mesh_plot_native --
  --test-threads=1 --nocapture` — 8 passed, covering native
  Surface3d/revolve rendering, clipping, wireframe, resize/equal-aspect
  behavior, native framebuffer/export parity, and field-only telemetry with
  no geometry upload.
- WGPU/cache, embedded viewport, and compute differential integration tests —
  7, 7, and 8 passed.
- WGPU embedded viewport/readback integration tests — 7 passed, including the
  1,000-update connected-grid memory/upload contract and same-layout cell-field
  replacement in both 2D and 3D.
- `cargo test --offline -p gpui-d3rs --features gpu-3d --test mesh_3d_cache_tests` — 10
  passed, including WGPU 3D scalar-storage and vertex/cell field-layout
  contract assertions plus stale field- and geometry-write rejection.
- `cargo test -p gpui-python-runtime` — 124 passed (120 unit tests, 4 integration tests).
- `cargo test -p gpui-python-runtime --features showcase --bin gpui-python-showcase` — 30 passed.
- `cargo test --offline -p gpui-python-runtime --lib` — 123 passed, including
  MeshFrameStore transient-failure recovery and bounded-cache regressions.
- `cargo test -p gpui-python-runtime --features showcase,native-qa --bin
  gpui-python-showcase native_host_shutdown -- --test-threads=1 --nocapture` —
  1 passed; direct native host cleanup cancels profiler subscriptions, clears
  MeshPlot ownership and decoded frames, and is safe to repeat.
- `cargo test -p gpui-python-runtime --features showcase --bin
  gpui-python-showcase runtime_resource_release -- --test-threads=1 --nocapture` —
  6 passed; shared `DropResource` handling correctly releases audio-only and
  mesh-only generations, reports unknown handles, maintains one reference per
  active plot for shared generations, and covers snapshot/frame recovery.
- `cargo test -p gpui-python-runtime --features showcase --bin
  gpui-python-showcase
  python_ir_showcase::runtime_resource_release_tests::native_patch_transaction_keeps_newest_valid_resources_and_rejects_late_updates
  -- --exact --test-threads=1 --nocapture` — 1 passed; the native host commits
  a valid field generation and ownership change, rejects missing resources and
  stale revisions, and preserves the newest committed app state.
- `cargo test -p gpui-python-runtime --features showcase --bin
  gpui-python-showcase
  python_ir_showcase::runtime_resource_release_tests::native_snapshot_transaction_replaces_owners_and_preserves_last_valid_state
  -- --exact --test-threads=1` — 1 passed; snapshot replacement, invalid
  snapshot preservation, missing-frame arrival, and same-ID fallback-cache
  retention are covered.
- `cargo test -p gpui-python-runtime --features showcase --bin
  gpui-python-showcase
  python_ir_showcase::runtime_resource_release_tests::native_mesh_frame_decode_errors_are_localized_to_the_retained_plot
  -- --exact --test-threads=1 --nocapture` — 1 passed; malformed binary mesh
  payloads stay out of the retained store and remain localized to the active
  plot.
- `cargo test --offline -p gpui-python-runtime --features native-qa --bin
  gpui-python-showcase runtime_resource_release_tests` — 8 passed, including
  generation-aware error routing, snapshot transactions, and malformed-frame
  recovery.
- The new rendered malformed-frame recovery test compiles, but its native
  process exits during host-wide macOS Text Input Services teardown before the
  test harness reports a result; it is therefore not counted as passed.
- `cargo test -p gpui-python-runtime --features showcase --bin
  gpui-python-showcase
  python_ir_showcase::runtime_resource_release_tests::native_mesh_frame_decode_errors_are_localized_to_the_retained_plot
  -- --exact --test-threads=1 --nocapture` — 1 passed; malformed binary mesh
  payloads stay out of the retained store and remain localized to the active
  plot.
- `cargo test -p gpui-python-runtime --features showcase --bin
  gpui-python-showcase python::tests -- --test-threads=1 --nocapture` — 5
  passed; the stdout reader preserves binary audio payloads and following
  `DropResource` messages while retaining malformed/stale recovery behavior.
- `cargo test --offline -p gpui-python-runtime --features showcase` — 221 passed
  across the library (143), host binary (37), showcase binary (37), and
  integration tests (4); doc-tests also completed with zero tests.
- `cargo test --offline -p gpui-python-runtime --features showcase --bin
  gpui-python-showcase python_ir_showcase::runtime_resource_release_tests --
  --test-threads=1 --nocapture` — 11 passed, including malformed inline field
  recovery, the integrated
  non-rendering native recovery matrix and post-recovery stale-frame
  rejection.
- `cargo test --offline -p gpui-python-runtime --features showcase --lib` — 143
  passed, including shared native decoder inline/resource validation tests.
- `cargo test --offline -p gpui-python-runtime --features showcase --lib
  native_mesh_plot::tests -- --test-threads=1` — 8 passed, including the
  invalid-camera transactional state-preservation regression.
- `cargo test --offline -p gpui-python-runtime --lib mesh_plot_selection_camera
  -- --test-threads=1` — 1 passed, including transactional axes and
  missing-value-policy property updates.
- `cargo test --offline -p gpui-python-runtime --lib mesh_plot_ --
  --test-threads=1` — 4 passed, including transactional revolve-property
  updates and the existing MeshPlot generation contracts.
- `cargo test --offline -p gpui-python-runtime --lib meshplot::tests --
  --test-threads=1` — 9 passed, including validated revolve axes, sweep,
  segment, cap, and view-ownership rules.
- `cargo test --offline -p gpui-python-runtime --lib -- --test-threads=1` —
  128 passed after extending MeshPlot IR validation and patch support.
- `cargo test --offline -p gpui-python-runtime --features showcase --bin
  gpui-python-showcase -- --test-threads=1` — 38 passed, including the
  session reader, MeshPlot decode, patch, recovery, and resource-ownership
  regressions.
- `cargo test --offline -p gpui-python-runtime --features showcase --bin
  gpui-python-showcase -- --test-threads=1` — 35 passed across the showcase
  and session regression modules.
- `cargo test --offline -p gpui-python-runtime --features showcase --bin
  gpui-python-showcase resource` — 15 passed, covering resource decoding,
  generation-aware routing, snapshot/patch transactions, and reader recovery.
- The eviction regression confirms that a field patch referencing an evicted
  generation preserves the newest valid committed app state and records a
  plot-local correlated error.
- `rustfmt --edition 2024 --check` passed for the shared decoder and showcase
  integration files.
- The showcase-feature binary now compiles without dead-code warnings for the
  native MeshPlot path; the shared runtime builder is the sole construction
  route.
- `cargo test -p gpui-python-runtime --features showcase,native-qa --bin
  gpui-python-showcase -- --test-threads=1 --nocapture` — native Python
  showcase suite previously passed after stabilizing the native text/platform
  setup. A current rerun passes the non-rendering cases and then terminates in
  `native_metal_invalid_resource_patch_keeps_the_last_valid_meshplot_frame`
  without a Rust diagnostic; this host-level Metal failure is separate from the
  focused shutdown regression.
- `cargo test -p gpui-python-runtime --features showcase,native-qa --bin gpui-python-showcase native_host_resource_ownership_keeps_active_plot_resources_until_release -- --test-threads=1` — 1 passed.
- `cargo check -p gpui-python-runtime` after native resource-owner wiring — passed.
- `cargo test --offline -p gpui-python-runtime mesh_frames --lib` — 13 passed (110
  filtered), including malformed-header/metadata errors, duplicate and split
-  chunk assembly, same-generation recovery after transient mismatch and
  budget-pressure failures, explicit reference counting, budget-pressure
  rejection, and 1,000 alternating field generations with retained geometry
  and bounded native storage.
- `PYTHONPYCACHEPREFIX=/private/tmp/gpui-toolkit-pycache /opt/homebrew/bin/python3.10 -m py_compile crates/gpui-python-runtime/python/gpui_toolkit/app.py crates/gpui-python-runtime/python/tests/test_showcase_session.py` — passed.
- `cargo test -p gpui-python-runtime --features native-qa --bin gpui-python-showcase native_host_resource_ownership_keeps_active_plot_resources_until_release -- --test-threads=1` — 1 passed.
- `PYTHONPATH=crates/gpui-python-runtime/python /opt/homebrew/bin/python3.10 -m unittest discover -s crates/gpui-python-runtime/python/tests -p 'test_showcase_session.py'` — 5 passed, including retained field/geometry replacement, inline/resource cross-products, camera/viewport/selection patch and reset operations, request correlation, future schema-version handling, malformed-message recovery, heartbeat continuity, and shutdown callback execution.
- `PYTHONPATH=crates/gpui-python-runtime/python /opt/homebrew/bin/python3.11 -m unittest discover -s crates/gpui-python-runtime/python/tests -p 'test_resources.py'` — 7 passed, including 1,000 alternating field generations with retained geometry and bounded Python storage.
- `PYTHONPATH=crates/gpui-python-runtime/python /opt/homebrew/bin/python3.11 -m unittest discover -s crates/gpui-python-runtime/python/tests` — 180 passed.
- `cargo test -p gpui-python-runtime --features showcase --bin gpui-python-showcase python::tests::reader_recovers_after_malformed_patch -- --nocapture` — 1 passed; a malformed patch is reported and a later heartbeat is still delivered.
- `cargo test -p gpui-python-runtime --features showcase --bin gpui-python-showcase python::tests -- --nocapture` — 2 passed; malformed and stale patches both leave later session messages deliverable.
- Script QA `test_qa_perf` with both required import paths — 8 passed.
- `PYTHONPATH=scripts python3 -m unittest scripts.tests.test_mesh_wgpu_manifest` — 6 tests run, 5 passed and 1 symlink test skipped because the host disallows usable symlink setup.
- `PATH=/Users/pierre/.cargo/bin:/opt/homebrew/bin:/usr/bin:/bin PYTHONPATH=scripts /opt/homebrew/bin/python3.11 -m unittest scripts.tests.test_qa_release_evidence scripts.tests.test_qa_perf scripts.tests.test_mesh_wgpu_manifest` — 27 passed, 1 symlink test skipped because the host disallows usable symlink setup.
- `/Users/pierre/.cargo/bin/cargo check -p gpui-d3rs --features gpu-2d,gpu-3d,gpu-metal` — passed.
- `cargo test -p gpui-d3rs --features gpu-3d --test mesh_3d_cache_tests` — 8
  passed, including the Metal/WGPU scalar-buffer contract assertions.
- `cargo check -p gpui-px --features gpu-3d` — passed after the WGPU 3D
  scalar-buffer split and dedicated MeshPlot toolbar-menu focus wiring.
- `cargo test -p gpui-px --features gpu-3d --lib color_scale` — 22 passed,
  including the protan/deutan/tritan color-scale distinguishability screen.
- `/Users/pierre/.cargo/bin/cargo test -p gpui-d3rs --features gpu-2d,gpu-3d --lib` — 1,019 passed.
- Focused follow-up suites — `gpui-px --features gpu-3d --lib mesh_plot`: 87
  passed; `gpui-d3rs --features gpu-2d,gpu-3d --lib offscreen`: 6 passed.
- `cargo run -p gpui-px --example mesh_plot_showcase --features gpu-3d` — 5 Sonium smoke scenarios passed.
- Isolated Metal MeshPlot visual capture/diff — 99 captured, 99 compared, 0
  failures, and 0 changed pixels.
- Baseline archive inspection — 102 members, 99 PNG captures, and 0
  AppleDouble sidecars.
- `bash -n scripts/qa_visual_capture.sh` — passed; a wrapper run reached
  native capture but its second macOS process exited during Text Input Services
  initialization, so that wrapper run is not release evidence. The focused
  serialized native suite above is the current native rendering evidence.
- QA script unit suite (`test_qa_*.py`) — 51 passed.
- Full script QA suite — 78 passed, including WGPU manifest schema/checksum,
  baseline, and skip-mode coverage.
- `bash -n scripts/qa_visual_capture.sh` — passed after adding freshness- and
  path-validated acceptance for renderer teardown statuses; stale reports are
  rejected rather than promoted as release evidence.
- A direct fresh one-case Metal capture — blocked by macOS
  `com.apple.hiservices-xpcservice` connection invalid teardown; no fresh
  capture report was written. The full fresh visual lane therefore remains
  open on this host.
- WGPU visual lane — explicit developer skip manifest produced because this
  host has no usable graphics adapter; required mode correctly rejects it.
- `PATH=/Users/pierre/.cargo/bin:/opt/homebrew/bin:/usr/bin:/bin
  CLANG_MODULE_CACHE_PATH=/private/tmp/gpui-toolkit-clang-cache just
  qa-cov-check` — passed at 73.50% (`90,825 / 123,568`).
- `qa_desktop_accessibility.py` — generated portable component/renderer
  evidence with MeshPlot accessibility registration; the report explicitly
  keeps VoiceOver/Narrator execution as `manual-required`.
- Focused coverage additions pass: MeshPlot validation, retained 2D/3D cell
  and vertex field paths, helper/domain/contour branches, builder setters,
  MeshPlot type constructors, revolve timing counters, and deterministic
  offscreen transform/clipping/colormap/line paths. The aggregate coverage
  floor is now green at 73.50% (`90,825 / 123,568`).
- `just qa-gpui-obvious` — design-token, component-lab, UI-kit, builder,
  audio-kit, and accessibility-contract portions passed; the recipe stopped
  when the macOS Metal build could not write the protected global Clang module
  cache, even when a writable temporary cache path was supplied.
- `scripts/qa_release_evidence.py` — developer-mode validation can consume the
  existing visual/conformance artifacts, but the fresh visual capture is not
  available on this host and `--require-clean` remains outstanding.
- `PATH=/Users/pierre/.cargo/bin:/opt/homebrew/bin:/usr/bin:/bin
  CLANG_MODULE_CACHE_PATH=/private/tmp/gpui-toolkit-clang-cache cargo test
  --workspace --lib --tests --quiet -- --test-threads=1` — the affected
  `gpui-scaffolder` suite had 20 tests pass; 3 unrelated scaffolder tests failed because the
  external `zed` Git dependency could not be resolved without network access.
- Touched-crate `cargo check` and `git diff --check` — passed.
- Current dirty-worktree benchmark collection contains 10 MeshPlot records;
  it is diagnostic evidence only and cannot satisfy clean release criteria.
- `gpui-d3rs` LOD benchmark slice — proxy upload ~7.9 µs, full restore upload
  ~301 µs, proxy field mapping ~5.6 ns, and controller transition ~0.9 ns for
  a connected 100k-triangle mesh; diagnostic CPU evidence only.

## Evidence refresh — 2026-08-16

- The focused release-evidence tests pass 14/14 after making the validator
  resolve `zstd`, `tar`, and Cargo tools from standard installation paths when
  a restricted shell omits them from `PATH`.
- The complete `test_qa_*.py` discovery passes 57/57.
- Developer-mode `scripts/qa_release_evidence.py` passes and writes a
  23-artifact manifest. The manifest is explicitly development evidence:
  `source_dirty: true`, and the recorded performance baseline/current artifacts
  have source-binding mismatches.
- `qa_perf_baseline.py --run gpui-d3rs:mesh_prep` now scopes its output to the
  requested benchmark instead of collecting stale records from other Criterion
  binaries; the regression suite covers this behavior. The optimized run
  produced 20 MeshPlot preparation records, but they remain dirty-worktree
  diagnostic evidence and are not a clean reference-machine gate.
- The WGPU visual wrapper now resolves Cargo and Python through task-scoped
  overrides or standard installation paths. Its restricted-PATH smoke run
  reaches the capture binary and records the expected developer skip because
  this host has no usable WGPU adapter; required release mode remains open.
  Both WGPU and Metal visual wrappers now choose a task-scoped writable Clang
  module cache when the caller has not supplied one, avoiding protected-cache
  failures before the renderer/adapter probe.
- The native rendered fallback test was reproduced serially and still aborts
  on the host with `com.apple.hiservices-xpcservice` connection-invalid during
  macOS Text Input Services teardown. This is an external host-runtime blocker,
  not a failed MeshPlot assertion; the rendered native matrix remains open.
- The native Python showcase binary lists 47 tests. Both focused rendered
  Metal tests terminate with status 1 before a Rust assertion is reported,
  while the non-rendering/resource recovery tests pass; this confirms the
  failure is shared by the rendered Python-host lane rather than specific to
  malformed-resource fallback.
- `cargo fmt --all -- --check` and `git diff --check` pass.
- `PYTHONPATH=scripts /opt/homebrew/bin/python3.11 -m unittest discover
  -s scripts/tests -p 'test_qa_*.py'` — 57 passed.
- Developer-mode `scripts/qa_release_evidence.py` passes and writes a
  23-artifact manifest to `/private/tmp`; `--require-clean` correctly fails
  because this implementation worktree is intentionally dirty.
- `scripts/qa_release_evidence.py --require-clean` remains a release blocker;
  clean visual-baseline promotion, fresh native capture, WGPU reference
  baselines, and reference-machine performance evidence are still required.

## Evidence refresh — 2026-08-17

- Native camera parsing now completes before retained-state mutation. The new
  regression rejects a malformed 3D camera while preserving zoom, selection,
  style, color range, revisions, and orbit state; the native MeshPlot test
  module passes 9/9.
- `cargo test --offline -p gpui-python-runtime --features showcase --lib --
  --test-threads=1` — 146 passed.
- `cargo test --offline -p gpui-px --features gpu-3d --lib mesh_plot --
  --test-threads=1` — 93 passed, including retained-state and validation
  coverage.
- `cargo check --offline -p gpui-px --no-default-features` and
  `cargo check --offline -p gpui-python-runtime --no-default-features` —
  passed, confirming the configuration snapshot compiles across feature
  boundaries.
- `cargo test --offline -p gpui-python-runtime --features showcase --bin
  gpui-python-showcase runtime_resource_release_tests -- --test-threads=1` —
  11 passed, including malformed inline field recovery and late
  superseded-generation frame rejection after
  recovery.
- `cargo test --offline -p gpui-python-runtime --features showcase --
  --test-threads=1` — 225 passed across library (147), host (37), showcase
  (37), and integration tests (4).
- Rustfmt for `src/native_mesh_plot.rs` and `git diff --check` pass.

- The adapter-backed WGPU viewport suite now passes 8/8. The new 60-frame
  camera-only test confirms that camera updates retain geometry and scalar
  storage with one geometry upload and zero scalar writes; this is local
  adapter evidence, not the reference-machine 60 Hz gate.
- Added and passed same-layout cell-field replacement coverage for both 2D and
  3D WGPU custom draws. Each update reuses the retained geometry resource and
  writes the expanded triangle-local scalar payload.
- `cargo test -p gpui-d3rs --features gpu-3d --test
  mesh_wgpu_embedded_viewport -- --test-threads=1` — 8 passed on the real
  adapter-backed lane.
- `cargo test -p gpui-px --features native-qa --test mesh_plot_native --
  --test-threads=1 --nocapture` — 8 passed; all rendered cases report the
  explicit no-Metal-device developer skip.
- `cargo test -p gpui-python-runtime --features showcase,native-qa --bin
  gpui-python-showcase native_mesh_plot_tests -- --test-threads=1 --nocapture`
  — 10 passed; the six rendered cases report the same explicit developer skip
  and the four non-rendering lifecycle cases execute normally.
- `cargo check -p gpui-px --example mesh_plot_showcase --features gpu-3d` and
  the default smoke run pass. The opt-in window run reaches the GPUI process;
  the host then emits the known Text Input Services/launch-services errors and
  is terminated by the bounded startup probe, so live visual QA remains open.
- `cargo test -p gpui-px --test mesh_plot_accessibility -- --test-threads=1` —
  3 passed, including configured axis-label/unit, selection, range, disabled-
  control announcements, and toolbar control order/disabled state.
- `cargo test -p gpui-ui-kit --test integration_tests menu --
  --test-threads=1` — 21 passed, including named menu/item accessibility,
  checked/disabled state export, and existing menu/context-menu coverage.
- `cargo test -p gpui-python-runtime --lib
  session_restart_clears_revision_and_mesh_generation_history` — passed.
- Focused native-qa lifecycle tests for session reset and recovery both pass
  serially in `gpui-python-showcase`.
- `cargo test -p gpui-python-runtime --lib mesh_plot -- --test-threads=1` —
  4 generation/transaction tests passed, including removed-plot cleanup.
- The showcase recovery matrix passes with an assertion that removing the
  plot clears its session generation history.
- `cargo test -p gpui-d3rs --features gpu-3d --lib` — 1,020 passed.
- `PYTHONPATH=scripts /opt/homebrew/bin/python3.11 -m unittest discover
  -s scripts/tests -p 'test_qa_*.py'` — 57 passed.
- `cargo fmt --all -- --check`, `git diff --check`, and shell syntax checks for
  both visual wrappers pass.
- The current host still cannot promote WGPU visual baselines or revalidate
  native rendered Metal/Python cases: no usable WGPU adapter or compatible
  Metal device is available. The native suites now skip cleanly instead of
  terminating the process. Manual accessibility and live UX review,
  reference-machine 60 Hz/performance evidence, and clean release evidence
  remain open.

## Release completion criteria

The feature is ready for product integration only when:

- [~] Native Metal and WGPU 3D/revolve paths have cross-adapter visual
  evidence. Native Metal and WGPU adapter-backed cases, manifests, and
  baselines pass; exact cross-adapter pixel comparison remains outstanding.
- [~] Field/style updates have runtime upload and memory evidence. Native Metal
  and embedded WGPU tests now verify upload/write counts, bytes, resident
  allocation estimates, and submission timings; long-run eviction and
  asynchronous GPU-memory evidence remain open.
- [ ] Python session and resource patch behavior is covered end to end.
- [ ] Invalid input preserves the newest valid rendered frame.
- [ ] SVG, PNG, and native framebuffer output have verified parity across the
  supported adapters. The covered native Metal cases pass; cross-adapter
  parity remains outstanding.
- [~] Visual artifacts exist for all registered MeshPlot variants and the
  six-scene WGPU baseline passes strict manifest validation, but the 99-case
  Metal archive is not a current zero-diff baseline: the latest comparison is
  72/99. Regenerate and bind it after the source revision is clean.
- [ ] VoiceOver and Narrator QA is complete.
- [~] Reference-machine performance demonstrates 60 Hz navigation and no
  unnecessary geometry re-upload. The real-adapter 60-frame budget and
  1,000-update retained-memory checks pass; longer eviction and production GPU
  memory evidence remain outstanding.
- [ ] Sonium live examples work without an external viewer for routine mesh,
  contour, axisymmetric, and 3D surface inspection.

## Adapter-backed WGPU cell-field status — 2026-08-16

- [x] Make WGPU cell-associated shading portable. The WGSL path no longer uses
  unsupported `primitive_index`; cell uploads expand to triangle-local
  vertices, values, and wireframe indices before resource creation.
- [x] Keep scalar rejection portable across adapters. The 2D shader uses the
  same equality-and-range finite predicate as the 3D shader instead of WGSL
  `isNan`/`isInf` builtins, and the real adapter-backed regression passes.
- [x] Preserve retained update semantics. Same-layout field replacements write
  only the scalar buffer, while a vertex-to-cell layout change rebuilds the
  retained geometry resource exactly once. The adapter-backed viewport suite
  passes, including scalar-color differences between two cell values.
- [~] Complete the mode/theme matrix and promote a clean WGPU baseline. The
  local adapter regression is green; cross-adapter visual baselines and clean
  release evidence remain separate release gates.

## Verification refresh — 2026-08-16

- `cargo test -p gpui-d3rs --features gpu-3d --test
  mesh_wgpu_embedded_viewport` — 7 passed on the real adapter-backed lane,
  including the 1,000-update connected-grid memory/upload contract and
  same-layout cell-field replacement in both 2D and 3D.
- `cargo test -p gpui-px --features native-qa --test mesh_plot_native` — 8
  native Metal MeshPlot rendered tests passed, including 3D/revolve parity,
  resize/equal-aspect selection, clipping, wireframe, and deterministic
  framebuffer/export behavior.
- A fresh rerun of that native Metal command on the current host terminates
  during the first test with process status 1 and no Rust assertion output.
  The prior serialized 8/8 result is retained as the last known-good native
  Metal evidence; current-host rendered revalidation is blocked by the same
  macOS platform/runtime teardown failure seen in the Python-host lane.
- Network-enabled serial workspace gate — all workspace library and test
  targets passed. The earlier three scaffold failures were dependency-fetch
  environment failures, not product regressions.

## Verification refresh — 2026-08-17 (latest local run)

- [x] `PYTHONPATH=scripts /opt/homebrew/bin/python3.11 -m unittest
  discover -s scripts/tests -p 'test_qa_*.py'` — 59 tests passed.
- [~] The local WGPU visual wrapper still produces an explicit developer skip
  because this host has no usable graphics adapter; required mode rejects the
  skip as intended, so no WGPU baseline was promoted.
- [~] Native Metal and Python rendered targets remain hardware-gated on this
  host. The suites now terminate cleanly with explicit no-device skips, but
  this does not replace reference-machine rendered evidence.
- [ ] Product VoiceOver/Narrator review, manual color-vision-deficiency
  review, live Sonium interaction sign-off, reference-machine 60 Hz/memory
  evidence, and clean `scripts/qa_release_evidence.py --require-clean`
  validation remain open.

## Verification refresh — 2026-08-17 (current worktree)

- [x] `cargo test --offline -p gpui-d3rs --features gpu-3d --lib` — 1,021
  passed.
- [x] `cargo test --offline -p gpui-px --features gpu-3d --lib mesh_plot`
  — 93 passed.
- [x] `cargo test --offline -p gpui-python-runtime --features showcase` —
  225 passed across library, host, showcase, and integration targets.
- [x] The native Metal MeshPlot target passes 8/8 and the native Python
  MeshPlot target passes 10/10; all Metal-dependent cases explicitly skip on
  this host because no compatible device is available.
- [~] The embedded WGPU viewport target passes 8/8, with five adapter-backed
  cases explicitly skipped because no usable graphics adapter is available;
  the remaining three software-independent checks pass. This is harness
  evidence, not reference-adapter visual or performance evidence.
- [x] QA-script discovery passes 59/59. The clean-release validator and
  reference-machine/manual UX gates remain open as listed above.

## Verification refresh — 2026-08-17 (release validator)

- [x] Developer-mode `scripts/qa_release_evidence.py` generated a manifest
  successfully at `/private/tmp/meshplot-release-evidence.json`, recording
  revision `15ce711d4ed8e9a4ffd3436ac87e52959147c804` and
  `source_dirty: true`.
- [ ] Strict `scripts/qa_release_evidence.py --require-clean` remains
  intentionally blocked by the dirty implementation worktree before release
  artifacts can be accepted. A clean commit/worktree with reference-adapter
  and manual UX evidence is still required for product integration.

## Verification refresh — 2026-08-17 (adapter-backed release run)

- [x] Escalated native Metal MeshPlot execution on the Apple M4 Pro passes
  8/8; no Metal-dependent case is skipped when GPU access is available.
- [x] Escalated native Python MeshPlot/session execution passes 10/10,
  including malformed-resource fallback, corrected-generation recovery,
  typed selection, retained geometry replacement, and shutdown ownership.
- [x] Escalated WGPU embedded viewport execution passes 8/8, including
  scalar-buffer-only field updates, 1,000 field updates with bounded retained
  storage, clipped drawing, custom-draw cleanup, and framebuffer readback.
- [x] The six-scene WGPU visual capture produces a valid 256x192 manifest and
  passes actual-versus-baseline validation for mesh, smooth fill, flat cell
  fill, wireframe, isoline, and revolve.
- [~] The current 99-case Metal capture completes, but the checked-in archive
  compares 72/99 with zero changed pixels; the 27 failures are exactly the
  picking, revolve, and Surface3D variants whose baselines are stale relative
  to newly implemented visuals. Baseline promotion remains guarded until a
  clean source-bound revision is available.
- [~] Strict `scripts/qa_release_evidence.py --require-clean` validation and
  final artifact binding remain release gates; the current implementation
  worktree is not the clean source revision on which to promote new baselines.
- [x] The WGPU camera-only regression now measures 60 frames and enforces a
  one-second (60 Hz average) budget while asserting zero geometry/scalar
  uploads; the real-adapter test passes.
- [~] Exact cross-adapter pixel parity, Metal-compute parity/timing, longer
  reference-machine memory/eviction runs, and manual VoiceOver/Narrator,
  color-vision, and live Sonium UX review remain open.

## Verification refresh — 2026-08-17 (compute provenance)

- [x] `cargo fmt --all -- --check` and `git diff --check` pass.
- [x] `cargo test --offline -p gpui-d3rs --features gpu-3d,gpu-compute
  --test mesh_compute_diff_tests -- --test-threads=1 --nocapture` — 9
  passed, including the macOS adapter-provenance assertion that reports
  `wgpu::Backend::Metal` when the adapter-backed path is available.
- [~] The provenance assertion does not establish exact Metal-versus-CPU
  output parity or production GPU timing. Those remain open together with
  long-run memory/eviction evidence, exact cross-adapter pixel comparison,
  manual accessibility/CVD review, and live Sonium UX sign-off.

## Verification refresh — 2026-08-17 (production adapter timing)

- [x] `cargo check --offline -p gpui-d3rs --features gpu-2d,gpu-3d,gpu-metal`
  and `cargo check --offline -p gpui-px --features gpu-3d` pass after adding
  timing fields to `MeshSceneState` and `RetainedMesh3DStats`.
- [x] `cargo test --offline -p gpui-d3rs --features gpu-3d
  --test mesh_wgpu_embedded_viewport -- --test-threads=1 --nocapture` — 8/8
  passed. The real-adapter tests now require non-zero geometry, field-write,
  and retained-frame submission timing while preserving the 60-frame and
  1,000-update upload/memory contracts.
- [x] `cargo test --offline -p gpui-px --features native-qa --test
  mesh_plot_native -- --test-threads=1 --nocapture` — 8/8 passed on Metal,
  including native geometry, scalar-write, and retained-frame timing checks.
- [x] `cargo test --offline -p gpui-px --features gpu-3d --lib mesh_plot
  -- --test-threads=1` — 93 passed.
- [~] These timings measure CPU-side adapter resource creation and command
  submission, not asynchronous GPU execution. Exact cross-adapter pixels,
  GPU timestamp queries, long-run eviction, manual accessibility/CVD review,
  and live Sonium UX sign-off remain open.

## Verification refresh — 2026-08-17 (export timing)

- [x] Added `mesh_plot_png_frame` to the `gpui-px` Criterion matrix and the
  strict release workload contract. The focused benchmark completes at about
  1.10 ms per deterministic Surface3D PNG export on this host.
- [x] Promoted the new workload into both clean 30-record performance
  artifacts; `scripts/qa_release_evidence.py --require-clean` passes with the
  PNG export timing included in the release matrix.
- [x] The focused performance/release-evidence script suite passes 25/25.
- [~] The benchmark covers CPU-side projection and PNG encoding; asynchronous
  GPU execution timing and long-run adapter-memory eviction remain open.

## Verification refresh — 2026-08-17 (async GPU timing and generation churn)

- [x] WGPU retained 3D exposes optional adapter timestamp-query capability and
  period through `WgpuContext`; the diagnostic is enabled only with
  `SOTF_GPU_TIMESTAMPS=1`.
- [x] The isolated opt-in adapter test
  `adapter_3d_timestamp_queries_recover_async_gpu_duration` passes and
  recovers non-zero render-pass GPU duration through deferred readback.
- [x] The default adapter-backed WGPU viewport suite passes 10/10, including
  the new 100-generation geometry replacement memory-bound contract; normal
  rendering and readback remain unchanged when timestamp diagnostics are off.
- [x] `cargo check --offline -p gpui-d3rs --features gpu-2d,gpu-3d,gpu-metal`
  and `cargo check --offline -p gpui-px --features gpu-3d` pass.
- [x] The retained GPU unit slice passes 13/13 and the MeshPlot unit slice
  passes 93/93.
- [~] Metal-compute GPU timing/parity, driver-private allocation/eviction
  evidence, exact cross-adapter pixel parity, and manual accessibility/CVD/
  live-Sonium sign-off remain open.

## Current actionable task list — 2026-08-17

Status legend: `[x]` fully implemented and evidenced, `[~]` partially
implemented or awaiting acceptance evidence, `[ ]` not completed.

### Features

- [x] MeshPlot data model, validation, masks, contours/isoline handling,
  equal-aspect mapping, revolve preparation, retained BVH/picking, and
  deterministic SVG/PNG export.
- [x] Retained field-only updates are separated from geometry/topology updates
  across CPU, Metal, and WGPU, including flat cell-associated shading and the
  100-generation bounded-resident-memory regression.
- [x] Complete exact Metal/WGPU rendering parity for the canonical depth,
  clipping, HiDPI, resize, wireframe, isoline, revolve, orientation, masking,
  and high-contrast scenes at identical inputs and viewports. The reference
  host reports six cases, zero changed pixels, and zero failures; broader
  camera/range/selection/mask matrix coverage remains a separate task.
- [~] Complete the compatible-reference-host Python rendered-session matrix;
  invalid patches, stale generations, recovery, resource ownership, and
  restart behavior have focused host coverage, but full rendered acceptance
  remains a separate gate.
- [~] Establish exact Metal-compute versus CPU-reference output parity and
  production GPU execution timing where compute is supported. CPU differential
  coverage and opt-in adapter timestamp instrumentation now exist; direct
  Metal-compute output/timing evidence remains a reference-adapter gate.

### Testing

- [x] WGPU visual matrix contains and validates six cases: `mesh`, `smooth`,
  `cell`, `wireframe`, `isoline`, and `revolve`; the promoted `cell.png` and
  actual-versus-baseline checks pass.
- [x] Retained GPU unit tests pass 13/13; the MeshPlot unit slice passes
  93/93; the full offline `gpui-d3rs --features gpu-3d --lib` suite passes
  1,021/1,021; the supported Python 3.11 QA-script suite passes 62/62.
- [x] WGPU adapter tests cover field-only updates, vertex/cell layout changes,
  camera-only retention, bounded generation churn, framebuffer readback, and
  opt-in asynchronous render-pass timestamp recovery.
- [~] Add exact cross-adapter framebuffer/SVG/PNG comparison tests covering
  camera, displayed range, axes, selected annotations, and masked data. The
  reusable comparator and canonical WGPU comparison IDs are implemented and
  tested, the canonical reference-host captures now compare exactly, and the
  adapter viewport suite covers explicit camera, displayed range, and NaN-mask
  behavior (14/14). Native SVG/export tests cover axes and selected
  annotations; paired expanded state/annotation captures remain outstanding.
- [~] Extend asynchronous GPU timing to Metal compute and define its
  production diagnostic policy. Adapter compute timing now follows the same
  opt-in `SOTF_GPU_TIMESTAMPS=1` policy and reports completed durations; capable
  reference-adapter timing evidence is still required.

### QA

- [~] Promote and validate the six-scene WGPU baseline; the baseline, 99-case
  Metal archive, safe-path/checksum checks, and performance evidence are
  implemented and validated, and the reference-host six-case Metal/WGPU
  cross-adapter report passes exactly. Strict release binding still requires
  the complete artifact set and source revision to be clean and co-bound.
- [x] Keep native Metal/Python/WGPU harnesses, retained-state contracts,
  compute differential tests, session recovery tests, and QA validators green.
- [~] Complete manual color-vision-deficiency review for named scales, masked
  regions, isolines, selected annotations, and cell fill; automated
  protan/deutan/tritan checks are present.
- [ ] Perform manual VoiceOver and Narrator QA for graphic summaries,
  labels/ranges/selection announcements, toolbar roles/states, focus order,
  keyboard actions, and tooltip/selection contents.

### Performance and resource evidence

- [x] Maintain the named clean performance matrix, including 100k/200k
  preparation, contours, BVHs, revolve, LOD, retained field/camera frames,
  picking, deterministic PNG export, and 30 current records bound to the
  latest revision.
- [x] Maintain adapter-backed 60-frame camera budget, zero-upload camera
  retention, 1,000 field updates, bounded 100-generation geometry churn, and
  non-zero opt-in WGPU GPU-duration evidence.
- [~] Capture longer reference-adapter memory/eviction behavior, including
  driver-private allocations, cache eviction, alternating fields, and release
  after plot removal. Backend-independent peak residency and release counters
  now retain evidence across resource destruction; reference-driver evidence
  remains open.
- [~] Measure drag-time LOD proxy/full restoration on an adapter-backed path,
  including visual quality and picking correctness. Retained stats now expose
  proxy-active state, proxy/full transition counts, and proxy/full triangle
  counts plus CPU nanoseconds for each scene swap. The embedded WGPU suite now
  includes an adapter-backed proxy/full render and full-BVH ray-pick regression;
  reference-adapter frame budget, visual-quality thresholds, and live navigation
  evidence remain open.

### UX and product sign-off

- [~] Validate live GPUI interaction for planar mesh, smooth/cell fill,
  contours, axisymmetric/revolve, and 3D surface views: pan, zoom, fit,
  reset, orbit, selection, menus, focus return, and disabled capabilities.
- [ ] Complete live Sonium sign-off without an external viewer for routine
  mesh, contour, axisymmetric, and 3D surface inspection.
- [ ] Complete manual screen-reader and keyboard sign-off on the same live
  examples after reference-adapter visual review.
- [ ] Record final release evidence only after all incomplete items are closed
  and the six-scene WGPU manifest, Metal archive, performance artifacts, and
  source revision are bound to one clean commit.

## Verification refresh — 2026-08-17 (peak and release memory telemetry)

- [x] `MeshSceneState` now records peak adapter-owned resident bytes, peak
  scalar-buffer capacity, and non-empty resource-release count while keeping
  current residency separate from historical evidence.
- [x] WGPU 2D, WGPU retained 3D, and Metal renderer teardown clear current
  residency after unregistering their custom draw while preserving peak
  counters for post-destruction diagnostics.
- [x] The 100-generation WGPU regression asserts peak residency, and the new
  adapter-backed drop regression asserts current-memory release plus preserved
  peak evidence. Both remain explicit developer skips when no adapter exists.
- [x] Retained telemetry unit tests pass 3/3; `gpui-d3rs` feature checks and
  `gpui-px` GPU checks pass; compute differential tests pass 9/9; MeshPlot
  unit tests pass 93/93; the full `gpui-d3rs --features gpu-3d --lib` suite
  passes 1,021/1,021; formatting and `git diff --check` pass.
- [~] Peak/release telemetry still excludes driver-private allocations and
  does not replace long-run reference-adapter eviction or manual product QA.

## Verification refresh — 2026-08-17 (drag-time LOD telemetry)

- [x] Retained 3D stats now expose whether the drag proxy is active, proxy
  upload count, full-resolution restore count, proxy/full triangle counts, and
  CPU nanoseconds for proxy and full-resolution scene swaps.
- [x] The retained LOD regression verifies that a large mesh uses fewer proxy
  triangles, preserves full mesh bounds, keeps scalar upload cardinality
  aligned with proxy positions, restores the full upload after the drag, and
  records non-zero transition timings.
- [x] `cargo check --offline -p gpui-px --features gpu-3d` passes and the
  focused retained LOD test passes 1/1 after formatting.
- [~] Adapter-backed visual quality, drag-time frame budget, and full-resolution
  picking during live navigation still require reference-host evidence.

## Verification refresh — current continuation

- [x] `MeshCompute` compiles with opt-in adapter timestamp instrumentation;
  CPU/reference behavior remains unchanged when `SOTF_GPU_TIMESTAMPS` is not
  set to `1`.
- [x] `cargo test --offline -p gpui-d3rs --features gpu-3d,gpu-compute
  --test mesh_compute_diff_tests -- --test-threads=1` passes 10/10, including
  the timestamp-contract regression (which explicitly skips without a capable
  opted-in adapter).
- [x] WGPU visual wrapper reaches the capture example, writes the expected
  no-adapter skip manifest on this host, and validates it against the updated
  six-case schema; developer mode remains honest and release-required mode
  remains rejecting.
- [x] The embedded WGPU viewport suite now contains 12 tests, including an
  adapter-backed drag-LOD case that renders the proxy and restored full mesh,
  verifies proxy triangle reduction and bounds preservation, and confirms a
  full-resolution BVH ray hit after restoration. It explicitly skips when no
  adapter is available.
- [x] The retained LOD unit slice now passes with non-zero proxy-swap and
  full-restore timing counters; these are CPU transition diagnostics, while
  adapter-backed frame-budget and visual-quality evidence remain separate.
- [x] `cargo test --offline -p gpui-px --features gpu-3d --lib
  -- --test-threads=1` passes 527/527 after adding the retained LOD timing
  fields and regression assertions.
- [x] WGPU manifests now require canonical `comparison_id` values, and the
  focused Python manifest/release/comparator suite passes 32 tests with one
  expected symlink skip. The full script suite passes 98/98 with one expected
  symlink skip.
- [x] Strict release validation now requires
  `target/qa/visual/mesh-plot-cross-adapter.json` with six canonical IDs,
  distinct renderer labels, valid thresholds/metrics, safe repository-local
  image paths, and non-empty referenced images; developer mode may omit it.
- [x] `scripts/qa_mesh_cross_adapter_visual.sh` now provides the reproducible
  paired-manifest release lane, writes the deterministic report, revalidates
  custom report paths inside the repository, and skips honestly when the pair
  is unavailable; required mode fails when either manifest is missing.
- [x] The `just qa` aggregator runs the developer cross-adapter lane and
  `just qa-release-evidence` reruns it with `QA_CROSS_ADAPTER_REQUIRED=1`, so
  a release cannot silently proceed without the paired report.
- [~] The restricted non-escalated lane has no usable Metal/WGPU adapter, while
  the escalated reference-host lane below provides adapter-backed rendering
  evidence. Paired framebuffer parity, driver-private memory, adapter LOD
  visual quality, and manual UX gates remain separate acceptance items.

## Verification refresh — 2026-08-17 (Python MeshPlot validation coverage)

- [x] Added focused Rust regressions for invalid resource generations and
  split-resource completeness, unsupported views/modes/scales and field
  associations, contour/cell incompatibility, non-finite values, validity
  masks, dimensions, camera shape, geometry shapes/IDs, and duplicate or
  unsupported interactions.
- [x] `cargo test --offline -p gpui-python-runtime meshplot --lib
  -- --test-threads=1` — 14 passed, including the two new validation tests.
- [~] This closes a local validation-test coverage gap without changing the
  public validation contract; reference-adapter rendering, manual UX review,
  and clean release evidence remain separate gates listed above.

## Verification refresh — 2026-08-17 (reference-host adapter and visual QA)

- [x] Escalated native Metal MeshPlot QA passes 8/8, covering equal-aspect
  selection after resize, full/partial revolve rendering, asynchronous large
  revolve completion, deterministic export, keyboard interaction, clipping,
  and wireframe output.
- [x] Escalated native Python MeshPlot/session QA passes 10/10, including
  resource ownership, restart generations, shutdown, localized patch errors,
  malformed-frame recovery, retained geometry replacement, typed selection,
  and nested layout selection.
- [x] Escalated embedded WGPU QA passes 12/12, including field-only updates,
  camera-only retention, bounded memory, timestamp recovery, clipped viewports,
  drag-time LOD rendering/picking, and custom-draw cleanup.
- [x] Required WGPU visual capture passes all six canonical scenes; the
  dedicated Metal capture also passes all six canonical scenes, and the
  reference-host cross-adapter report passes 6/6 exactly.
- [~] The full component-lab lane captures all 99 Metal MeshPlot cases but
  currently reports 72/99 zero-pixel baseline diffs because 27 checked-in
  images are stale for picking, revolve, and Surface3D visuals. The full
  `gpui-d3rs` visual feature matrix passes 1,021/1,021; Python native/session
  QA passes 48/48 on the reference host.
- [x] Compute differential QA passes 10/10. Compute timestamp instrumentation
  now requires both `TIMESTAMP_QUERY` and `TIMESTAMP_QUERY_INSIDE_PASSES`, so
  adapters that cannot provide valid in-pass timestamps skip explicitly rather
  than recording zero-duration samples. The WGPU render timestamp lane remains
  adapter-backed and passing.
- [x] Fixed the `gpu-3d` integration-test wiring so the private timestamp type
  is not parsed by the directly included test renderer; the full visual lane
  now compiles and runs under its actual feature combination.
- [x] A dedicated six-case Metal manifest at the same 256x192 low-level scene
  contract as the WGPU manifest is implemented and paired with the WGPU
  manifest. The reference-host comparator reports six exact passes; the
  current 99-case Metal component-lab evidence remains a separate UX matrix.
- [~] Driver-private memory/eviction evidence, adapter LOD frame-budget and
  visual-quality thresholds, manual VoiceOver/Narrator and color-vision review,
  live Sonium UX sign-off, and final clean-commit release evidence remain open.

## Authoritative task list — 2026-08-17 expanded-matrix continuation

This section supersedes status statements in earlier verification snapshots.
`[x]` means fully implemented and evidenced, `[~]` means partially complete or
awaiting acceptance evidence, and `[ ]` means not completed. Manual or
reference-host-only evidence is not inferred from local no-adapter runs.

### Features

- [x] MeshPlot data model, validation, masks, contours/isoline handling,
  equal-aspect mapping, revolve preparation, retained BVH/picking, and
  deterministic SVG/PNG export.
- [x] Retained field-only updates remain separate from geometry/topology
  updates across CPU, Metal, and WGPU, including flat cell-associated shading,
  stale-result rejection, and bounded generation churn.
- [x] Canonical six-scene Metal/WGPU rendering parity is exact on the reference
  host, including depth, clipping, HiDPI, resize, wireframe, isolines,
  revolve, orientation triad, masking, and high-contrast scenes.
- [x] Expanded camera, displayed-range, and NaN-mask state capture is
  implemented, compared with the same exact-pixel contract, and covered by
  adapter-backed viewport regressions.
- [~] Expanded cross-adapter parity still needs paired selection annotations
  and axes captures. Native selection, axes, SVG, and accessibility contracts
  exist independently; live planar/axisymmetric selection now also renders a
  retained orange cell outline from the current viewport and selection state.
- [~] Python rendered-session acceptance is largely implemented and has
  focused recovery/ownership coverage, but the complete reference-host
  rendered matrix and product sign-off remain open.
- [~] Metal-compute output parity and production GPU execution timing have
  CPU differential coverage and opt-in instrumentation, but no final
  reference-adapter acceptance evidence.

### Testing

- [x] Core Rust and Python regressions cover mesh validation, contours,
  revolved geometry, retained dirty domains, BVH reuse, picking, masks,
  interaction state, invalid resources, recovery, and stale generations.
- [x] Canonical WGPU/Metal visual manifests and the six-case exact PNG
  comparator are validated; the shared comparator also has an explicit PNG/SVG
  artifact-kind contract and deterministic SVG canonicalization tests. The
  expanded report validator enforces the three canonical state IDs, exact
  thresholds, unique cases, and non-failed metrics.
- [x] The expanded validator has focused tests for valid reports, failed cases,
  duplicate/missing IDs, and malformed JSON. The complete Python QA-script
  discovery passes 105 tests with one expected skip.
- [x] Adapter-backed viewport tests cover explicit camera, displayed range,
  NaN masking, field-only updates, camera-only retention, bounded generation
  churn, readback, and opt-in timestamp recovery.
- [~] Add paired adapter captures for selected annotations and axes, then add
  the resulting IDs to the expanded release contract. The live planar
  selection layer and pure projection/rejection tests are now implemented, but
  this does not substitute for paired adapter framebuffer evidence.
- [~] Complete rendered Python subprocess/session cases on the compatible
  reference host rather than relying on non-rendering protocol coverage.

### QA and release evidence

- [x] The canonical visual lanes, 99-case Metal component-lab matrix, WGPU
  baseline, cross-adapter comparator, explicit artifact-kind checks,
  safe-path checks, and required-mode adapter behavior are implemented.
- [x] `qa_mesh_cross_adapter_expanded.sh` uses the shared expanded validator;
  developer mode skips honestly without adapters and `QA_EXPANDED_REQUIRED=1`
  fails when a required Metal/WGPU adapter is unavailable.
- [x] Developer evidence may omit the expanded report, while strict release
  evidence now requires and validates its three exact cases and repository-
  local non-empty images.
- [~] Regenerate and retain the expanded report/images from the reference host,
  then bind them to the same clean source revision as all other release
  artifacts. The current worktree is dirty and the local host has no usable
  Metal/WGPU adapter.
- [~] Complete manual CVD review for named scales, masked regions, isolines,
  selected annotations, and cell fill; automated protan/deutan/tritan checks
  are present.
- [ ] Perform manual VoiceOver and Narrator QA for summaries, labels, ranges,
  selection announcements, toolbar roles/states, focus order, keyboard
  actions, and tooltip contents.

### Performance and resource evidence

- [x] The named CPU performance matrix covers 100k/200k preparation, contours,
  BVHs, revolve, LOD preparation, retained field/camera frames, picking, and
  deterministic PNG export.
- [x] Adapter-backed evidence covers the 60-frame camera budget with zero
  geometry/scalar uploads, 1,000 field updates, bounded 100-generation
  replacement, teardown release counters, and opt-in WGPU GPU timestamps.
- [~] Capture longer reference-adapter memory/eviction behavior, including
  driver-private allocations, alternating fields, cache eviction, and release
  after plot removal.
- [~] Measure adapter-backed drag-time LOD frame budget, visual quality, and
  full-resolution picking during live navigation; CPU transition timings and
  retained WGPU proxy/full tests are already present.
- [~] Extend and accept Metal-compute GPU timing alongside the existing WGPU
  timestamp policy and CPU reference comparison.

### UX and product sign-off

- [x] Accessibility metadata, named toolbar/menu regions, keyboard contracts,
  focus return, disabled-state reporting, and export/selection annotations
  have automated coverage.
- [~] Validate live GPUI interaction for planar mesh, smooth/cell fill,
  contours, axisymmetric/revolve, and 3D surface views across pan, zoom, fit,
  reset, orbit, selection, menus, focus return, and disabled capabilities.
- [ ] Complete live Sonium sign-off for routine mesh, contour, axisymmetric,
  and 3D surface inspection without an external viewer.
- [ ] Complete manual screen-reader and keyboard sign-off on those live
  examples after reference-adapter visual review.
- [ ] Produce final clean-release evidence only after every partial/manual gate
  is closed and all visual, performance, UX, and source-revision artifacts are
  co-bound to one clean commit.

## Verification refresh — 2026-08-17 (live selection overlay)

- [x] Live planar and axisymmetric-section MeshPlot frames now retain and draw
  the selected cell outline through a transparent GPUI 2D overlay. The overlay
  follows the current visible x/y viewport and equal-aspect letterboxing, uses
  the retained mesh/triangle identity checks, and honors selection clearing
  without restoring a stale builder-time selection.
- [x] The overlay has focused pure projection/rejection coverage for valid
  cells, foreign mesh IDs, and invalid cell indices. The focused tests pass
  3/3; the full `gpui-px --features gpu-3d --lib` suite passes 530/530;
  `cargo check --offline -p gpui-px --features gpu-3d` and `git diff --check`
  pass.
- [~] The expanded adapter lane still needs a real product-level paired
  framebuffer harness for selection annotations and axes. The current
  low-level Metal/WGPU capture examples intentionally do not claim this
  evidence, and no reference-host result is inferred from the local
  no-adapter environment.

## Verification refresh — 2026-08-17 (cross-adapter artifact contract)

- [x] `scripts/mesh_plot_visual_compare.py` now accepts explicit `png` and
  `svg` artifact kinds. PNG cases retain the existing decoded pixel comparison;
  SVG cases use UTF-8 XML canonicalization, tolerate only representation-level
  ordering/whitespace differences, and require exact canonical equality.
- [x] The comparator rejects unknown kinds, kind/extension mismatches, mixed
  PNG/SVG pairs, missing artifacts, unsafe paths, malformed SVG, and corrupt
  PNG while preserving the legacy kind inference for existing `.png` manifests.
- [x] Focused comparator and release-evidence tests pass 32/32. The release
  validator now requires the canonical cross-adapter report and every case to
  declare `artifact_kind: png` with `.png` paths; the regenerated six-case
  reference report validates with 6/6 exact passes.
- [~] No paired product-level SVG capture report is claimed yet. Native
  MeshPlot SVG determinism/export tests and the generic SVG comparator are
  covered, but paired adapter SVG evidence remains part of the reference-host
  and product-level acceptance work alongside selection annotations and axes.

## Authoritative task list — fresh reference-host verification, 2026-08-17

This final section supersedes earlier snapshot claims in this document.
`[x]` means implemented and evidenced, `[~]` means partially complete or
awaiting acceptance evidence, and `[ ]` means not completed. Evidence from a
local no-adapter skip is not treated as reference-host evidence.

### Features

- [x] MeshPlot model and validation: indexed triangle meshes, vertex/cell
  fields, missing-value masks, contours/isoline preparation, equal-aspect
  mapping, axisymmetric revolve preparation, retained BVH/picking, and
  deterministic PNG/SVG export.
- [x] Retained updates: field-only changes avoid geometry/topology rebuilds
  across CPU, Metal, and WGPU; stale generations are rejected and bounded
  retained-resource behavior is covered.
- [x] Canonical Metal/WGPU rendering contract: the six mesh, smooth, cell,
  wireframe, isoline, and revolve scenes compare exactly on the reference host
  (Metal 6/6, WGPU 6/6, cross-adapter report 6/6).
- [x] Expanded camera, displayed-range, and NaN-mask adapter lane (3/3),
  plus live planar/axisymmetric selected-cell overlay and projection guards.
- [x] Product-level paired Metal/WGPU framebuffer regression harness now uses
  the same high-level `gpui-px::MeshPlot` tree with GPUI axes, labels, colorbar,
  wireframe, and selected-cell state. Adapter-backed headless Metal/WGPU
  captures verify non-flat output and selection changes on both adapters;
  release artifact emission remains tracked under QA below.
- [x] Reference-host Python MeshPlot/showcase session matrix passes 48/48;
  final Sonium product sign-off is tracked separately below.
- [x] Metal-compute CPU parity and production timing evidence are implemented:
  the compute test covers field ranges, isolines, filled bands, and opt-in GPU
  timestamps, while the strict release validator requires a Metal backend and
  non-zero timestamp samples.
- [~] Execute and retain that evidence on a clean reference Metal adapter; the
  local host records an explicit no-adapter skip.

### Testing

- [x] Core Rust and Python regressions cover validation, contours, revolve,
  masks, retained dirty domains, BVH reuse, picking, interaction state,
  invalid resources, recovery, and stale generations.
- [x] Reference-host focused lanes pass: native Metal MeshPlot 8/8, Python
  native/session QA 48/48, embedded WGPU viewport 14/14, expanded
  camera/range/mask adapter QA 3/3, and `mesh_3d_cache_tests` 10/10.
- [x] PNG/SVG artifact comparison and release validators are implemented and
  tested, including canonical SVG normalization, safe-path checks, duplicate
  or missing IDs, malformed artifacts, and required-mode failures.
- [~] The complete component-lab capture runs all 99 cases, but the current
  baseline comparison is 72/99 zero-pixel passes. The 27 failures are exactly
  the nine picking, nine revolve, and nine Surface3D variants with stale
  checked-in visuals.
- [x] Product-level paired axes/selection regression is covered by
  `mesh_plot_wgpu_product`: both adapter framebuffers are asserted non-flat,
  selection changes the framebuffer, and SVG output contains axis lines, axis
  titles, and the selected annotation.
- [x] The release-facing product capture lane now persists plain/selected
  Metal and WGPU PNGs plus a source-provenance manifest when adapters are
  available; its validator checks safe paths, dimensions, composition metadata,
  and non-zero selected-vs-plain pixel deltas.
- [~] Run that lane on the clean reference host and attach its four persisted
  images to the final release evidence set; this worktree can only produce the
  explicit no-adapter skip manifest.

### QA and release

- [x] Canonical six-scene Metal/WGPU manifests, exact cross-adapter report,
  artifact-kind checks, safe paths, and required adapter behavior are in
  place and passing on the reference host.
- [x] The release capture/manifest lane for the high-level GPUI product
  regression is implemented in `scripts/qa_mesh_product_visual.sh` and
  `scripts/qa_release_evidence.py`, including plain/selected Metal and WGPU
  images, axes and selection metadata, safe-path checks, and source-revision
  binding in strict mode.
- [~] Execute the strict product lane on a clean reference host so the
  captured manifest and four images become final release evidence.
- [x] The compute evidence lane is implemented in
  `scripts/qa_mesh_compute.sh` and `scripts/qa_release_evidence.py`; it
  validates backend identity, CPU parity, timestamp enablement, non-zero
  samples, and source provenance.
- [~] Execute the strict compute lane on a clean reference Metal adapter so
  the timing/parity JSON becomes final release evidence.
- [~] Regenerate/promote the 99-case Metal baseline only after implementation
  changes are final and a clean source-bound revision is available. Do not
  promote the current stale archive just to turn 72/99 into a green check.
- [~] Produce one clean release evidence set co-bound to the source revision,
  including the six-scene report, 99-case archive, expanded product report,
  and performance artifacts.
- [~] Complete manual color-vision-deficiency review for scales, masked
  regions, isolines, selection annotations, and cell fill; automated checks
  already exist.
- [ ] Complete manual VoiceOver/Narrator review of summaries, labels, ranges,
  selection announcements, toolbar/menu roles and states, focus order,
  keyboard actions, and tooltip contents.

### Performance and resources

- [x] Named CPU matrix and benchmark targets cover 100k/200k preparation,
  contours, BVHs, revolve, LOD preparation, retained field/camera frames,
  picking, and PNG export; the current artifact contains 29 workload records.
- [x] Reference-adapter contracts cover 60-frame camera retention with no
  unnecessary uploads, 1,000 field updates, bounded 100-generation
  replacement, teardown release counters, and opt-in WGPU GPU timestamps.
- [~] Capture long-run driver-private memory and eviction evidence, including
  alternating fields, cache eviction, and release after plot removal.
- [~] Measure adapter-backed drag-time LOD frame budget, visual-quality
  thresholds, and full-resolution picking during live navigation; CPU
  transition timings and retained proxy/full tests already exist.
- [x] Extend the accepted GPU timing/parity contract to the Metal-compute path
  and bind its evidence JSON into the release manifest.
- [~] Capture the strict Metal-compute artifact on the clean reference host;
  unsupported local adapters remain an explicit skip.
- [x] Metal renderer telemetry now records the platform driver's
  `currentAllocatedSize` snapshot and retained peak through
  `MeshSceneState`/`RetainedMesh3DStats`, with native Metal assertions for
  non-zero current and monotonic peak allocation evidence.

### UX and product sign-off

- [x] Automated accessibility metadata, named toolbar/menu regions, keyboard
  contracts, focus return, disabled states, export metadata, and selection
  annotations are covered.
- [~] Complete live GPUI interaction review for planar mesh, smooth/cell fill,
  contours, axisymmetric/revolve, and 3D surface views across pan, zoom, fit,
  reset, orbit, selection, menus, and disabled capabilities.
- [ ] Complete live Sonium sign-off for mesh, contour, axisymmetric, and 3D
  surface inspection without an external viewer.
- [ ] Complete manual screen-reader and keyboard sign-off on those live
  examples.
- [ ] Mark the feature release-ready only after the open QA, performance, UX,
  and clean source/artifact-binding tasks above are closed.

## Verification refresh — 2026-08-17 (product-level paired GPUI capture)

- [x] Added `crates/gpui-px/tests/mesh_plot_wgpu_product.rs`, which builds the
  same high-level MeshPlot composition for Metal and WGPU and exercises GPUI
  axes, labels, colorbar, wireframe, and selected-cell rendering through the
  adapter-backed headless window path.
- [x] The regression asserts non-flat Metal/WGPU framebuffers, a framebuffer
  change when selection is enabled on each adapter, and SVG axis/title/
  selection metadata. It passes 1/1 on the adapter-capable verification host;
  the local no-adapter run also passes by taking the explicit skip path.
- [x] The focused product test and the full `gpui-px --features gpu-3d --lib`
  suite pass (`1/1` and `530/530`); `cargo fmt --all -- --check` and
  `git diff --check` also pass.
- [x] The product test now supports persisted screenshots and a release-facing
  manifest when `GPUI_MESH_PLOT_PRODUCT_CAPTURE_DIR` is set; the validator and
  shell wrapper are recorded in the next refresh.

## Verification refresh — 2026-08-17 (persisted product evidence lane)

- [x] `scripts/qa_mesh_product_visual.sh` now builds the product test, runs its
  test binary directly with capture/provenance environment variables, persists
  `metal/plain.png`, `metal/selected.png`, `wgpu/plain.png`, and
  `wgpu/selected.png`, and validates the resulting manifest. Direct binary
  execution is required because this Cargo test runner did not forward the
  custom capture variables in the restricted shell.
- [x] The release validator includes the product manifest as a required
  artifact, hashes the four images when captured, validates axes/colorbar and
  selection metadata, independently decodes PNGs, and requires a non-zero
  selected-vs-plain pixel delta for both adapters.
- [x] Focused release-evidence tests pass 22/22; the comparator tests pass
  12/12; product Rust test passes 1/1; the full `gpui-px --features gpu-3d
  --lib` suite passes 530/530; formatting, shell syntax, and `git diff --check`
  pass.
- [~] This host has no usable Metal/WGPU adapter, so the developer lane writes
  and validates a skipped manifest. Strict mode correctly rejects that skip;
  clean reference-host capture and final artifact promotion remain open.

## Verification refresh — 2026-08-17 (Metal-compute evidence lane)

- [x] Added `crates/gpui-d3rs/tests/mesh_compute_evidence.rs`, which compares
  adapter field min/max, isolines, and filled bands against the CPU reference,
  records the adapter backend, and records opt-in GPU timestamp state.
- [x] Added `scripts/qa_mesh_compute.sh`; it builds and directly executes the
  evidence test with `SOTF_GPU_TIMESTAMPS=1`, persists
  `target/qa/perf/mesh-compute-gpu.json`, writes an explicit skip when no
  adapter-backed compute device is available, and supports strict release
  rejection of non-Metal or untimestamped evidence.
- [x] Release-evidence tests pass 23/23 and the compute evidence test passes
  1/1 locally through its explicit no-adapter path; formatting, shell syntax,
  and `git diff --check` remain clean.
- [~] A clean reference Metal host must still produce the captured manifest
  with all parity flags true and non-zero GPU timestamp samples.

## Verification refresh — 2026-08-17 (Metal driver allocation telemetry)

- [x] Metal custom draws now publish `MTLDevice.currentAllocatedSize` into
  retained MeshPlot diagnostics, preserve the peak snapshot across resource
  churn, and clear the current snapshot on renderer teardown. WGPU remains
  explicitly `None` because it has no portable driver-private allocation API.
- [x] Native Metal test compilation passes with the new telemetry assertions;
  the existing reference-host native suite will now fail if current or peak
  driver allocation evidence is absent.
- [~] Long-run eviction and driver-private behavior still require a clean
  reference Metal run that alternates large fields/generations and records the
  telemetry over time; this no-adapter host cannot produce that artifact.

## Verification refresh — 2026-08-17 (persisted LOD and Metal memory evidence lanes)

- [x] Added `crates/gpui-d3rs/tests/mesh_lod_evidence.rs` and
  `scripts/qa_mesh_lod.sh`. The lane exercises a connected-grid drag proxy
  through the adapter-backed retained custom draw, persists proxy/full PNGs,
  records 60 drag frames, checks the average frame budget, and verifies
  full-resolution restoration telemetry.
- [x] Added `validate_mesh_lod_evidence` to
  `scripts/qa_release_evidence.py`; it checks adapter identity, source
  provenance, proxy reduction, PNG dimensions/non-black output, frame-budget
  samples, and restoration counters. Strict release mode requires Metal,
  captured images, and a passing budget.
- [x] Added `crates/gpui-d3rs/tests/mesh_metal_memory_evidence.rs` and
  `scripts/qa_mesh_metal_memory.sh`. The lane alternates field writes and
  geometry generations, records `MTLDevice.currentAllocatedSize`/peak/resident
  samples, and proves teardown clears current allocation while preserving peak
  evidence.
- [x] Added `validate_mesh_metal_memory_evidence` to
  `scripts/qa_release_evidence.py`; it checks 20 positive monotonic samples,
  lifecycle provenance, alternating churn, release count, current-memory
  clearing, and peak retention. Strict release mode requires a clean Metal
  artifact.
- [x] Wired both lanes into `Justfile` developer QA, strict release QA, and
  shell-syntax checks. Local developer runs produce explicit validated skip
  manifests because this host has no usable WGPU/Metal adapter; no
  adapter-backed timing, PNG, or driver-private allocation claim is made from
  those skips.
- [x] Verification: release-evidence Python tests `25/25`; the LOD test binary
  compiles and its developer lane validates a skip; the Metal-memory test
  binary compiles and its developer lane validates a skip; `cargo fmt --all`
  and shell syntax checks pass.
- [~] Run both new lanes on a clean Metal reference host, retain the captured
  images/samples, and bind both artifacts to the final clean source revision.
  This remains the adapter-backed evidence gate; the LOD and telemetry
  instrumentation/validation contracts are implemented.

## Verification refresh — 2026-08-17 (product paired-state contract and CVD screen)

- [x] High-level product captures now assign stable comparison IDs for the
  axes composition (`px.mesh_plot.product.axes`) and selected annotation
  (`px.mesh_plot.product.selection`) cases for both Metal and WGPU images.
- [x] The product manifest now persists paired Metal/WGPU changed-pixel
  metrics for axes and selection, and the release validator checks the case
  mapping, safe PNG artifacts, finite metrics, and composition metadata.
  This is an acceptance contract, not a claim of exact text-raster parity.
- [x] Named MeshPlot color scales have deterministic protan/deutan/tritan
  simulation coverage in `color_scale::tests::named_scales_remain_distinguishable_under_cvd_simulations`.
  The test is an automated regression screen; the human CVD review remains
  open for rendered scales, masks, isolines, cell fill, and annotations.
- [x] The product capture test compiles and its developer QA lane validates
  the explicit no-adapter path; the CVD regression passes independently.
- [~] A compatible reference host must still retain the paired product PNGs
  and strict source-bound manifest, and manual CVD review must still record
  real rendered stimuli before those QA gates can close.

## Verification refresh — 2026-08-17 (Python/session and accessibility audit)

- [x] `gpui-python-runtime --features showcase --lib` passes `152/152`,
  including MeshPlot resource ownership, stale-generation rejection,
  transactional patches, restart history reset, and retained field/geometry
  dirty-domain coverage.
- [x] `gpui-px --test mesh_plot_accessibility --features gpui` passes `3/3`,
  covering the structured graphic summary, selection/range metadata, toolbar
  roles, disabled export state, and keyboard/render order.
- [x] Release-evidence and desktop-accessibility validator tests pass `30/30`.
- [~] These automated contracts do not substitute for compatible reference-host
  rendered Python acceptance, VoiceOver/Narrator execution, or live Sonium
  product sign-off; those gates remain explicitly open.

## Verification refresh — 2026-08-17 (supported-toolchain QA rerun)

- [x] The focused release-evidence tests pass 26/26, the manifest/comparator
  tests pass 24 tests with one expected symlink skip, and the high-level
  product capture test compiles with
  `native-qa,headless-qa`.
- [x] The product, compute, drag-time LOD, and Metal-memory developer lanes
  all complete successfully and validate explicit no-adapter manifests on
  this host; none of those skips is promoted to reference-adapter evidence.
- [x] The complete Python QA-script discovery passes 119 tests with one
  expected skip when run with the supported Python 3.14 toolchain and Cargo
  available on `PATH`; shell syntax, `cargo fmt --all -- --check`, and
  `git diff --check` also pass.
- [~] The remaining release gates are unchanged: clean reference-host product
  PNGs and source-bound manifests, strict Metal compute/LOD/memory artifacts,
  stale 99-case Metal baseline review, manual CVD review, VoiceOver/Narrator
  QA, live GPUI interaction review, Sonium sign-off, and final clean-release
  artifact binding.

## Verification refresh — 2026-08-17 (release-contract cleanup)

- [x] Added the required [`qa/visual/mesh-plot-cvd-qa.md`](qa/visual/mesh-plot-cvd-qa.md)
  manual rendered-stimulus checklist. It records the scale, mask, isoline,
  cell-fill, selected-annotation, colorbar, and layout/theme review surface;
  the automated color-scale simulation remains a necessary but insufficient
  regression screen.
- [x] Updated the 99-case component-lab diff validator so developer evidence
  accepts all 99 complete cases when the checked-in archive is known to be
  stale, while `--require-clean` still requires zero failed cases and zero
  changed pixels. Added a regression covering both modes.
- [x] Corrected `qa/visual/mesh-plot-reference.md` to report the current
  72/99 zero-diff and 27 stale-baseline differences instead of claiming a
  clean comparison. Developer `scripts/qa_release_evidence.py` now completes
  and writes `target/qa/release-evidence.json`; strict mode correctly stops at
  the intentionally dirty worktree.
- [x] Verification: complete QA-script discovery passes 119 tests with one
  expected skip; focused release-evidence tests pass 28/28 and the CVD screen
  regression passes 1/1; product, compute, LOD, and Metal-memory developer
  lanes pass; `cargo fmt --all -- --check` and `git diff --check` pass.
- [x] Strict visual-diff validation now also requires persisted repository-local
  baseline, actual, and diff PNG paths for every case, while developer mode
  retains compatibility with older diagnostic reports. The focused validator
  regression covers the missing-path failure.
- [x] Added the dependency-free `scripts/qa_mesh_cvd.py` and
  `scripts/qa_mesh_cvd.sh` lane. When product PNGs are captured it simulates
  protan, deutan, and tritan output, checks all four product cases and
  selected-vs-plain distinguishability, records source provenance, and marks
  human review as still required. The lane is wired into `Justfile` developer
  and strict-release QA; no-adapter runs produce an explicit validated skip.
- [x] CVD screen regression passes, and the release validator now requires a
  captured source-bound CVD report in strict mode while accepting an explicit
  developer skip. This strengthens rendered QA without incorrectly closing
  the manual CVD acceptance gate.

## Verification refresh — 2026-08-17 (keyboard fit capability)

- [x] MeshPlot keyboard handling now accepts `f`/`fit` as the fit action when
  the declared `PlotInteractions` capability allows `fit`. Planar plots
  restore the same finite initial domain used by the toolbar; live 3D plots
  pass current surface/revolve bounds into the camera fit path. Disabled
  capabilities leave the camera or viewport unchanged.
- [x] Updated the interaction QA report to name keyboard fit coverage and
  added a renderer-free regression for enabled/disabled fit behavior.
- [x] The focused MeshPlot interaction slice passes `23/23`, including the
  planar and 3D bounds-aware fit regressions. The full GPUI-PX library run
  passes `532/532`; `cargo check --offline -p gpui-px --features gpu-3d`,
  formatting, and `git diff --check` also pass.
- [~] Live GPUI pointer/keyboard review across every MeshPlot view and
  compatible-reference-host screen-reader/Sonium sign-off remain manual
  acceptance gates; this change does not infer those results from unit tests.

## Final task list — source-bound release closeout, 2026-08-17

This is the current consolidated status. `[x]` means fully implemented and
verified, `[~]` means implemented but missing reference-host or manual
acceptance evidence, and `[ ]` means not completed. A local no-adapter skip or
the intentionally dirty worktree is never counted as release evidence.

### Features

- [x] MeshPlot core behavior: indexed triangle validation, vertex/cell fields,
  missing-value masks, contours/isobands, equal-aspect mapping, revolve,
  retained BVH/picking, selection overlays, and deterministic PNG/SVG export.
- [x] Retained rendering behavior: field-only updates avoid geometry rebuilds;
  geometry, camera, masking, axes, interaction, and stale-generation changes
  use distinct dirty domains across CPU, Metal, and WGPU.
- [x] Adapter contracts: canonical six-scene parity, expanded camera/range/
  mask cases, product-level axes/colorbar/selection captures, compute parity
  instrumentation, drag-time LOD instrumentation, and Metal allocation
  telemetry are implemented.
- [x] Python resource/session recovery and accessibility metadata contracts are
  implemented, including transactional patches, restart handling, keyboard fit,
  focus/disabled-state metadata, and named toolbar/menu regions.

### Testing

- [x] Rust/Python regression coverage is in place for validation, contours,
  revolve, masks, retained updates, BVH/picking, interaction, invalid resource
  recovery, stale generations, accessibility, and export metadata.
- [x] Latest verification: `gpui-px` library 532/532, MeshPlot interaction
  23/23, accessibility target 3/3, focused release-evidence tests 31/31, and
  QA-script discovery 126 tests with one expected skip.
- [x] Canonical six-scene, expanded state, product paired-state, compute,
  LOD, Metal-memory, CVD, and strict-artifact-path validator contracts have
  focused automated tests.
- [~] The 99-case component-lab capture is complete, but the checked-in visual
  archive currently compares 72/99 cases cleanly; 27 cases remain stale and
  must be regenerated from the final source revision.
- [~] Adapter-backed and rendered Python lanes have executable recovery and
  skip behavior, but final compatible-reference-host capture evidence still
  needs to be retained and promoted.

### QA and release evidence

- [x] Developer and strict release validators enforce artifact schemas, safe
  repository-local paths, case counts, checksums, renderer identity, image
  dimensions, source provenance, and clean source-revision binding.
- [x] `scripts/qa_visual_capture.sh` persists `source_revision` and
  `source_dirty`; strict visual capture/diff validation rejects missing, dirty,
  or mismatched provenance. The regression suite covers these failure modes.
- [x] CVD review runbook and automated rendered CVD screen are present; the
  automated lane records that human review remains required.
- [~] Produce one clean, source-bound release evidence set containing the
  regenerated 99-case Metal archive, canonical/expanded/product captures,
  CVD report, and all required platform artifacts.
- [ ] Complete manual VoiceOver and Narrator review for summaries, ranges,
  selection announcements, toolbar/menu roles, focus order, keyboard actions,
  and tooltip contents.

### Performance and resource evidence

- [x] Named CPU benchmarks cover 100k/200k preparation, contours, BVHs,
  revolve, LOD preparation, retained field/camera frames, picking, and PNG
  export; the current workload artifact contains 29 records.
- [x] Retained performance contracts cover 60-frame camera retention, zero
  unnecessary uploads, 1,000 field updates, bounded 100-generation churn,
  teardown release counters, and opt-in GPU timestamp diagnostics.
- [x] Metal-compute parity/timing, drag-time LOD, and Metal driver-allocation
  evidence lanes plus strict validators are implemented.
- [~] Capture strict non-zero Metal-compute timing, adapter-backed LOD frame
  budget/visual-quality evidence, and long-run Metal private-allocation/
  eviction evidence on a clean compatible reference host.

### UX and product sign-off

- [x] Automated UX contracts cover pan/zoom/orbit/reset/fit capability wiring,
  selection metadata, keyboard fit, focus return, disabled controls, export
  metadata, accessibility summaries, axes/labels, colorbar, and menu semantics.
- [~] Perform live GPUI interaction review across planar, smooth/cell,
  contour, axisymmetric/revolve, and 3D views, including selection and
  keyboard/pointer behavior.
- [ ] Complete live Sonium sign-off for routine mesh, contour, axisymmetric,
  and 3D inspection without an external viewer.
- [ ] Complete manual screen-reader and keyboard sign-off on the live product.
- [ ] Mark MeshPlot release-ready only after every partial/manual task above is
  closed and all artifacts are co-bound to one clean source revision.

## Verification refresh — 2026-08-17 (release provenance binding)

- [x] Visual capture and diff reports now carry source revision and dirty-state
  provenance; strict validation requires a clean matching revision and persisted
  repository-local baseline, actual, and diff image paths.
- [x] Added regression coverage for missing provenance, dirty provenance,
  mismatched revisions, stale complete developer diffs, and product-source
  mismatches.
- [x] Verification passed: release-evidence tests 29/29, complete QA-script
  discovery 120 tests with one expected skip, `cargo fmt --all -- --check`,
  and `git diff --check`.
- [~] Final clean release validation remains open until the reference-host
  captures, regenerated 99-case baseline, performance artifacts, manual UX
  reviews, and one clean source revision are available together.

## Verification refresh — 2026-08-17 (visual QA skip and stale-diff handling)

- [x] `scripts/qa_visual_capture.sh` now recognizes the component-lab's
  structured no-adapter report, rewrites it as an explicit developer skip, and
  emits a matching skipped diff artifact. Strict release validation continues
  to reject both skipped artifacts.
- [x] The developer visual wrapper now accepts a complete 99-case diff with
  known stale baseline differences, while still rejecting incomplete reports.
  The current run reports the 99-case matrix honestly and does not promote
  the 27 stale cases.
- [x] End-to-end developer visual QA passes on this host: renderer-independent
  and GPU tests, design-token conformance, 147-story component-lab conformance,
  showcase inventories, and explicit no-Metal-adapter capture/diff skips.
- [x] Developer release evidence generation passes after the skip
  normalization; focused release-evidence tests pass 31/31, the complete
  QA-script suite passes 122 tests with one expected skip, and shell syntax,
  formatting, and `git diff --check` pass.
- [~] A compatible reference host is still required for the 99 actual Metal
  images, zero-diff baseline promotion, strict product/compute/LOD/memory
  artifacts, and final clean source binding.

## Verification refresh — 2026-08-17 (feature matrix)

- [x] `gpui-px` now gates the GPUI-dependent `surface3d` module and re-export
  on both `gpui` and `gpu-3d`; the crate compiles without default features for
  both `gpu-2d` and `gpu-3d` configurations.
- [x] Verification passed: `cargo check --offline -p gpui-px
  --no-default-features --features gpu-2d`, the equivalent `gpu-3d` check,
  the normal `--features gpu-3d` check, and `cargo fmt --all -- --check`.
- [~] This closes a local feature-matrix compile gap only; it does not reduce
  the remaining compatible-reference-host or manual product sign-off gates.

## Verification refresh — 2026-08-17 (release RC toolchain resolution)

- [x] `scripts/release_rc.py` now resolves `cargo`, `rustc`, `rustup`, and
  `just` from `~/.cargo/bin` when a restricted shell omits that directory from
  `PATH`, including the package-wave subprocesses.
- [x] The complete Python QA-script discovery now passes `123` tests with one
  expected skip; the reproducible release-candidate bundle test passes again.
- [~] This removes a local reproducibility defect. It does not create the
  missing clean reference-host Metal/WGPU captures or manual UX evidence.

## Verification refresh — 2026-08-17 (expanded-lane stale-artifact guard)

- [x] `scripts/qa_mesh_cross_adapter_expanded.sh` now removes its prior
  Metal/WGPU manifests and comparison report before capture. An adapter skip
  therefore cannot leave stale captured artifacts that a later developer or
  release validation run could mistake for current evidence.
- [x] Re-ran the expanded lane on this host: it reported the unavailable Metal
  adapter and left no expanded manifests or comparison report. Developer
  compute, LOD, Metal-memory, product, CVD, WGPU, and release-evidence lanes
  also validated successfully, with adapter-dependent outputs explicitly
  marked skipped where hardware was unavailable.
- [~] The guard improves evidence hygiene but does not close the actual
  reference-adapter capture requirement.

## Verification refresh — 2026-08-17 (canonical adapter-skip handling)

- [x] Canonical Metal/WGPU capture lanes now clear their owned manifests
  before probing, and the cross-adapter wrapper clears its owned comparison
  report before checking the pair.
- [x] Cross-adapter developer QA now distinguishes captured, explicit skipped,
  malformed, and unknown manifest statuses. Skipped pairs exit cleanly in
  developer mode; required release mode rejects them; malformed or unknown
  statuses always fail.
- [x] Added three subprocess regressions covering developer skip handling,
  required-mode rejection, stale-report removal, and unknown-status rejection.
- [x] Verification passed: full QA-script discovery `126` tests with one
  expected skip, developer release-evidence generation, all MeshPlot shell
  syntax checks, formatting, and `git diff --check`.
- [~] These orchestration improvements still cannot substitute for actual
  reference-adapter captures or manual product/accessibility acceptance.

## Verification refresh — 2026-08-17 (strict completion audit)

- [x] The current developer release manifest still passes after the latest
  lane changes, while `qa_release_evidence.py --require-clean` fails closed at
  the intentionally dirty worktree boundary.
- [x] `QA_CROSS_ADAPTER_REQUIRED=1` rejects the current skipped Metal/WGPU
  pair, and the focused lane/release-evidence regression slice passes `34/34`.
- [~] The strict failures are the expected remaining external gates: a clean
  source revision, real adapter captures, strict performance artifacts, and
  manual accessibility/product acceptance.

## Verification refresh — 2026-08-17 (reference-adapter continuation)

- [x] The detected Apple M4 Pro reference adapter ran the required canonical
  Metal and WGPU visual lanes: six Metal scenes, six WGPU scenes, and the
  six-case exact cross-adapter report all passed with zero failed cases.
- [x] The expanded camera, displayed-range, and NaN-mask lane passed all three
  exact cross-adapter cases. The product-level axes/colorbar/selection capture
  test also passed through both Metal and WGPU; its developer artifact is
  intentionally marked dirty because this implementation worktree is dirty.
- [x] Adapter-backed performance tests passed: Metal compute parity/timing
  1/1, drag-time LOD 1/1, and Metal allocation/churn/teardown 1/1. Developer
  manifests validate successfully; strict validators reject the same
  artifacts only at the clean-source binding boundary.
- [x] Native Metal MeshPlot framebuffer/export/interaction QA passes 8/8,
  native Python rendered-session/resource recovery QA passes 10/10, and the
  embedded WGPU viewport suite passes 14/14 on the reference adapter.
- [x] Full renderer-backed visual QA captured all 99 registered Metal
  component-lab MeshPlot cases. The current comparison is complete with 72
  zero-pixel passes and 27 known stale-baseline differences; no capture is
  missing. Design-token conformance, 147-story component-lab conformance, and
  the automated CVD screen also pass.
- [x] Fixed two release-lane defects found by the reference run. The LOD
  evidence target now uses the platform WGPU color format instead of a
  hard-coded RGBA format, and evidence scripts select the newest built test
  binary rather than an arbitrary stale artifact. Metal memory evidence now
  forces each headless frame through screenshot rendering before sampling
  driver allocation telemetry.
- [x] The broad offline workspace Rust gate passes with
  `GPUI_SCAFFOLDER_OFFLINE=1`, including 23/23 scaffolder tests. The macOS
  pasteboard lane reports 3 passed and 5 explicitly ignored tests because a
  live pasteboard service is required.
- [x] Final affected-crate checks pass: `gpui-px` library 532/532,
  `gpui-d3rs --features gpu-3d` library 1,021/1,021, complete QA-script
  discovery 126 tests with one expected skip, all shell syntax checks,
  `cargo fmt --all -- --check`, and `git diff --check`.
- [~] Strict `scripts/qa_release_evidence.py --require-clean` still fails
  closed at the intentionally dirty worktree boundary. The 99-case baseline
  promotion and all captured performance/product artifacts must be regenerated
  or retained from one clean source revision before they count as release
  evidence.
- [~] Automated CVD checks pass, but manual CVD review remains required for
  named scales, masked regions, isolines, selected annotations, and cell fill.
- [ ] Manual VoiceOver/Narrator review, live GPUI pointer/keyboard review, and
  live Sonium sign-off remain outstanding.

## Verification refresh — current continuation (2026-08-17)

- [x] Re-ran `cargo test --offline -p gpui-px --features gpu-3d --lib
  -- --test-threads=1`: 532 passed, 0 failed.
- [x] Re-ran `cargo test --offline -p gpui-d3rs --features gpu-3d --lib
  -- --test-threads=1`: 1,021 passed, 0 failed.
- [x] Re-ran `cargo test --offline -p gpui-python-runtime --features showcase
  -- --test-threads=1`: 232 passed across the library, host, showcase, and
  integration targets; doc-tests completed with zero tests.
- [x] Re-ran `cargo fmt --all -- --check` and `git diff --check`; both pass.
- [x] Re-ran the focused QA-script discovery
  (`test_qa_*.py`): 75 tests passed. The expected temporary-repository
  diagnostics are informational and do not indicate failures.
- [x] Re-ran the complete QA-script discovery (`test_*.py`): 126 tests
  passed with one expected skip.
- [~] `scripts/qa_release_evidence.py --require-clean` still fails at the
  intentional dirty-worktree guard. No provenance or baseline claim is being
  promoted from this uncommitted implementation tree.
- [ ] The remaining acceptance work is unchanged: clean source-bound artifact
  promotion, manual CVD review, VoiceOver/Narrator review, live GPUI
  pointer/keyboard review, and live Sonium sign-off.

## Verification refresh — explicit WGPU product backend (2026-08-17)

- [x] Found and fixed a real product-capture gap: a macOS build containing
  `gpu-metal` selected the Metal custom draw by compile-time configuration even
  when the product test was using a WGPU headless renderer. The WGPU image
  therefore contained axes/selection but no scalar surface.
- [x] Added public `MeshPlotBackend::{Auto,Wgpu}` selection. `Auto` preserves
  the platform default; `Wgpu` routes planar retained draws and 3D custom
  draws through the WGPU renderer and marks a backend switch as a geometry
  dirty change so retained owners cannot be reused across adapters.
- [x] Strengthened `mesh_plot_wgpu_product` to select the intended backend and
  assert scalar fill at the plot center. The reference-adapter test passes
  1/1, and `scripts/qa_mesh_product_visual.sh` validates the regenerated
  Metal/WGPU product artifacts; both adapters now show the scalar surface.
- [x] Verification after the fix: `cargo check --offline -p gpui-px
  --features gpu-3d`, `cargo test --offline -p gpui-px --features gpu-3d
  --lib` (532/532), the reference native Metal suite (8/8), and the
  native/headless product target compile and pass.
- [~] The regenerated product artifacts are developer evidence from the dirty
  implementation worktree. Clean source binding, stale 99-case baseline
  promotion, and manual CVD/accessibility/live-product sign-off remain open.

## Verification refresh — 2026-08-17 (WGPU headless text and clear contract)

- [x] Fixed a real high-level WGPU product-capture defect: the headless scene
  encoder now flushes pending glyph-atlas uploads before submitting sprite
  batches. Previously the scalar surface and vector axes rendered, but the
  title and y-axis text were absent from WGPU screenshots.
- [x] The WGPU headless render target now honors the requested transparency
  mode. `transparent=false` clears an opaque black background, matching the
  Metal product lane and preventing transparent pixels from being mistaken for
  missing composition.
- [x] Added product raster assertions for title/y-axis text and opaque capture
  backgrounds. The reference-adapter product test passes 1/1, and both Metal
  and WGPU captures now contain the same measured title/y-axis pixel counts
  (2,113 and 2,592 respectively) with an opaque corner pixel.
- [x] `gpui_wgpu` headless-qa check, `gpui-px` product-test compilation,
  `gpui-px` MeshPlot unit tests (98/98), developer release-evidence generation,
  formatting, and `git diff --check` pass.
- [x] The dependency-free rendered CVD screen was rerun over the corrected
  four-image product set and its developer report validates for protan,
  deutan, and tritan simulations; the human review flag remains explicit.
- [~] The strict product wrapper still rejects the regenerated manifest at the
  clean-source provenance boundary because this implementation worktree is
  intentionally dirty. Clean artifact binding, baseline promotion, and manual
  CVD/accessibility/live-product sign-off remain open.

## Verification refresh — 2026-08-17 (current worktree regression audit)

- [x] Re-ran `/Users/pierre/.cargo/bin/cargo test --offline -p gpui-px --features gpu-3d --lib -- --test-threads=1`: 532/532 passed.
- [x] Re-ran `/Users/pierre/.cargo/bin/cargo test --offline -p gpui-d3rs --features gpu-3d --lib -- --test-threads=1`: 1,021/1,021 passed.
- [x] Re-ran `/Users/pierre/.cargo/bin/cargo test --offline -p gpui-python-runtime --features showcase -- --test-threads=1`: 232 tests passed across library, host, showcase, and integration targets.
- [x] Re-ran Python QA-script discovery: 126 tests passed with one expected skip; `cargo fmt --all -- --check` and `git diff --check` pass.
- [x] Developer release-evidence generation passes, and the CVD/product/compute/LOD/Metal-memory lane scripts fail closed or validate explicitly when this host has no usable adapter.
- [~] The current host cannot regenerate compatible-reference Metal/WGPU captures: expanded, product, compute, LOD, and Metal-memory outputs are explicitly skipped. Existing canonical paired captures remain prior reference-host evidence and are not promoted from this dirty worktree.
- [~] `scripts/qa_release_evidence.py --require-clean` still fails exactly at the intentional dirty-worktree boundary; clean source binding, final reference artifacts, and manual CVD/accessibility/product reviews remain open.

## Verification refresh — 2026-08-17 (continuation audit)

- [x] The active goal remains correctly open; no completion status was asserted while required evidence is missing.
- [x] Developer release-evidence generation still passes; strict `--require-clean` still fails at the intentional dirty-worktree check.
- [~] Current product, compute, LOD, and Metal-memory manifests remain explicit adapter skips (`no usable Metal adapter`, `no usable WGPU adapter`, or no adapter-backed compute device).
- [~] The canonical Metal/WGPU manifests remain captured prior-reference evidence; the expanded report is absent after its no-adapter stale-artifact guard, so neither lane is promoted as clean release evidence.
- [~] TokenSave interaction audit found automated coverage for export, picking, contour/revolve, camera, keyboard, focus, and accessibility; no additional implementable contract gap was identified.
- [ ] Manual CVD, VoiceOver/Narrator, live GPUI interaction, and Sonium product sign-off remain required before release-ready status.

## Verification refresh — 2026-08-17 (focused high-risk coverage)

- [x] `cargo test --offline -p gpui-px --features gpu-3d --lib mesh_plot::interaction::tests -- --test-threads=1`: 23/23 passed, covering keyboard navigation and fit, retained dirty domains, stale contour/revolve workers, BVH/picking reuse, LOD, and camera-only updates.
- [x] `cargo test --offline -p gpui-px --features gpu-3d --test mesh_plot_accessibility -- --test-threads=1`: 3/3 passed, covering summaries, native registration, and keyboard-order toolbar controls.
- [x] `cargo test --offline -p gpui-python-runtime --features showcase native_mesh_plot::tests -- --test-threads=1`: 9/9 passed, covering resource validation, axes/interactions, renderer options, camera rollback, and revolve settings.
- [x] Coverage audit found direct tests for `handle_3d_key`, `mark_resources_changed`, and native MeshPlot options; the GPUI-context-heavy `build_frame` has no separate direct unit mapping, but no missing contract or failing behavior was identified.
- [~] These focused tests strengthen automated evidence only; they do not replace clean source-bound adapter captures or manual CVD, screen-reader, live interaction, and Sonium acceptance.

## Verification refresh — 2026-08-17 (required native QA semantics)

- [x] Native Metal test helpers in `gpui-px` and Python MeshPlot showcase now honor `QA_NATIVE_REQUIRED=1` and `QA_METAL_REQUIRED=1`: unavailable adapters are hard failures in required lanes instead of silent passing early returns.
- [x] Developer native runs still complete without a compatible adapter, preserving the explicit skip behavior; the GPUI native lane reports 8/8 test cases and the Python native lane 5/5 test cases under the developer policy.
- [x] Required-mode regression was exercised: `QA_NATIVE_REQUIRED=1` fails all 8 GPUI cases and all 5 Python cases with the expected `native Metal QA requires a compatible Metal device` diagnostic.
- [x] `QA_METAL_REQUIRED=1 bash scripts/qa_mesh_metal_visual.sh` fails closed when the capture example cannot obtain a Metal adapter, rather than producing release evidence from a skip.
- [~] This closes a QA false-green path but does not create the missing compatible-reference-host captures; clean provenance, adapter evidence, and manual acceptance remain open.

## Verification refresh — 2026-08-17 (adapter evidence required-mode propagation)

- [x] Compute and Metal-memory evidence tests now hard-fail when `QA_COMPUTE_REQUIRED=1` or `QA_METAL_REQUIRED=1` and the required adapter is unavailable; developer runs still emit explicit skip manifests.
- [x] The LOD shell lane now propagates `QA_LOD_REQUIRED=1` into the WGPU test as `QA_WGPU_REQUIRED=1`, so required WGPU absence fails at the test boundary instead of only during later manifest validation.
- [x] Required shell lanes were exercised: compute, LOD, and Metal-memory each failed with the expected unavailable-adapter diagnostic; non-required compute/LOD/Metal-memory lanes each validated successfully with developer skips.
- [x] `cargo fmt --all -- --check` and `bash -n` checks for all three lane scripts pass.
- [~] This improves release-gate truthfulness but does not supply the compatible reference adapter, clean source provenance, or manual UX/product acceptance required for completion.

## Verification refresh — 2026-08-17 (live product target audit)

- [x] The sibling Sonium repository was checked for a live integration target. Its `crates/app-gpui` source contains no MeshPlot references, and its `Cargo.toml` resolves toolkit crates from remote Git revisions rather than this uncommitted worktree.
- [~] The shipped Sonium binary/source therefore cannot provide authoritative sign-off for this worktree's MeshPlot changes. Live GPUI interaction, screen-reader review, and Sonium product acceptance still require a compatible integrated build or an operator-provided product session.

## Verification refresh — 2026-08-17 (capture example audit)

- [x] Metal and WGPU capture examples return explicit adapter errors rather than writing captured manifests on unavailable hardware.
- [x] Their shell wrappers convert those errors into developer skip manifests or required-lane failures through manifest validation; no additional capture-example fix is needed.
- [~] The wrappers therefore provide truthful automation, but they cannot manufacture the compatible reference-host captures required for clean release evidence.
